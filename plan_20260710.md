# IQ Direct Optimization Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Keep the Cockpit Desktop one-click path on IQ direct and reduce USRP transport latency without changing the working QPSK path.

**Architecture:** IQ direct remains `cockpit_desktop -> server.py -> usrp_runtime.py -> RunAnalogLatentBatch.py -> persistent TX/RX -> board decode-server -> board-side TVM`. The first implementation pass adds stage-level metrics and guards. Later passes overlap RX capture, board decode, and TVM consumption while keeping the remote decoded directory contract intact.

**Tech Stack:** Python 3, Electron/React Cockpit Desktop, USRP292x persistent control servers, Phytium board Python virtual environment, TVM handwritten operator path, ML-KEM/auth channel, Docker for Linux/bash commands on Windows host.

## Global Constraints

- Do not modify the QPSK runner, QPSK decoder behavior, or QPSK cockpit defaults unless a shared-interface test proves the IQ change broke them. Treat QPSK as the regression baseline.
- Keep the USRP data plane off Tailscale. Tailscale may carry cockpit control, status, SSH, and logs, but IQ sample transport must stay on the direct USRP path.
- Use Docker for bash-driven validation from Windows. If Docker is blocked, use Windows Git Bash. Do not use WSL.
- Keep board login credentials in local environment variables only. Activate the board virtual environment before board-side Python commands: `source /home/user/venv/bin/activate`.
- Keep `JSCC_LINK_MODE=iq-direct` and `transport_mode=usrp` for IQ work. Do not let a previous QPSK run leak into the active state.
- Keep ML-KEM/authentication out of the per-image hot path. Measure security-on overhead separately after the transport path is stable.
- Commit after each completed task. Push after every three new local commits or when explicitly asked.

## Current Baseline

Latest cockpit-button-equivalent 300-image runs:

- 2026-07-11 latest accepted reliability fix validation: `batch-1783730551-300` stayed `300/300`, fallback `0`, after adding per-probe `probe_id` matching to the persistent path-probe worker and retrying transient SSH failures during remote capture directory precreation. TVM median/p95/max was `241.69/244.45/280.68 ms`; IQ total median/p95/max was `155.66/274.33/11124.64 ms`; PSNR was `37.0445`, SSIM was `0.97494`. This proves the stale same-request soft-probe response and one-off `kex_exchange_identification` precreate failure are fixed for the Cockpit-equivalent path. It is still not the final performance target because IQ p95 remains about `29.9 ms` above TVM p95, with remaining tails from true board `.npy` write stalls and delayed file visibility/soft-probe completion.
- 2026-07-11 pause checkpoint: the recommended Cockpit IQ direct profile remains the `batch-1783730551-300` profile above: Docker SSH/TX on the Windows host, board `/home/user/venv/bin/python`, IQ direct, handwritten TVM on big.LITTLE, `.npy` decoded latent output, minimal response-only summary, precreated capture dirs, persistent soft completion `0.05 s`, RX same-session batch control enabled with window `16`, and stable STOP drain defaults. This is acceptable for demo continuity because the median transport path is far below TVM (`155.66 ms` versus `241.69 ms`), but it is not a final p95 win.
- 2026-07-11 decoded-output placement A/B: moving decoded outputs toward `/tmp` produced one good 50-image smoke (`batch-1783735194-50`, IQ p95 `218.61 ms`), then an invalid 300-image run (`batch-1783735331-300`, `299/300`, missing `00000218.npy`) caused by RX not-armed / STOP recovery. After adding the STOP-timeout recovery below, `batch-1783735946-300` stayed `300/300`, fallback `0`, but IQ median/p95/max was `156.46/321.45/11549.86 ms` versus TVM `240.71/243.72/256.27 ms`. Do not promote the `/tmp` decoded-output placement.
- 2026-07-11 valid `ANALOG_REMOTE_DECODE_SOFT_COMPLETE_SEC=0.02` A/B: the 50-image run `batch-1783736840-50` looked healthy (`50/50`, IQ p95 `233.32 ms`), but the 300-image gate `batch-1783736949-300` stayed all-pass with IQ median/p95/max `160.57/279.31/13125.42 ms`, TVM median/p95/max `240.71/243.72/256.27 ms`, and one recovered retry. Lowering soft completion from `0.05` to `0.02` reduced worker-response p95 but moved the remaining tail to RX arm/capture, so keep `0.05` as the stable default. Note: this env must be set at backend process startup; posting board-access does not override it.
- 2026-07-11 RX batch-session A/B: increasing `ANALOG_RX_BATCH_SESSION_MAX_IMAGES` from `16` to `32` worsened a 50-image smoke (`batch-1783737367-50`, IQ p95 `391.46 ms`), and disabling batch session control was worse (`batch-1783737536-50`, IQ p95 `616.43 ms`, `57` records for `50` images). Keep RX batch session enabled with window `16`; do not rerun these variants unless new instrumentation identifies a different failure mode.
- 2026-07-11 STOP-timeout recovery fix: when `STOP` itself times out or raises after RX busy/timeout, `stop_rx_capture()` now continues polling `STATUS` until the RX server reports idle or the configured drain deadline expires. This targets the hardware case where STOP delivery succeeds but the client sees `ERR_TIMEOUT`, avoiding an immediate retry while the RX server is still busy. Local coverage: `test_stop_rx_capture_polls_status_after_stop_timeout`.
- 2026-07-11 offline tail audit on accepted baseline `batch-1783730551-300`: `33/300` records exceeded TVM p95 `244.45 ms`, so the final p95 win requires reducing a broad mid-tail, not only the four multi-second maxima. Among those 33 records, `15` had runner-side RX capture/control overhead above `50 ms` while server capture stayed near `64 ms`, `17` had decode/worker wait above `160 ms`, `5` had worker wait more than `80 ms` above board-reported decode when board-reported timing was present, `3` had true server capture above `100 ms`, and only `1` had a decoded-output write above `100 ms`. The next optimization should therefore target RX control response scheduling and soft-completion/worker wait behavior before revisiting decoded-output placement.
- 2026-07-11 code-only diagnostic follow-up: `batch_spool_summary.json` now includes `iq_tail_audit`, a summary-only classifier for total-transport tail counts, runner-side RX control overhead, true server capture stalls, decode/worker wait, worker-over-reported delay, write stalls, and soft-completed records. It uses optional `ANALOG_IQ_TAIL_REFERENCE_MS` for the over-reference count; without that env it still emits fixed `250/275/500 ms` tail counters. This does not change the IQ hot path or default profile.
- 2026-07-11 Cockpit visibility follow-up: `usrp_runtime.py` now preserves `iq_tail_audit` in the wrapper summary, `server.py` exposes it as `iq_tail_audit` / `batch_iq_tail_audit`, and the Cockpit security panel shows a compact IQ tail audit block after batch completion. This is diagnostics-only and does not change QPSK, IQ transport behavior, or timing defaults.
- 2026-07-11 opt-in RX health handling: `RunAnalogLatentBatch.py` now supports `ANALOG_RX_HEALTH_RESET_ON_STALL=1` / `--rx-health-reset-on-stall` plus `ANALOG_RX_HEALTH_STALL_THRESHOLD_SEC` to recycle the shared batch RX control session after a successful image whose `rx_wait_wall_sec` or `rx_capture_wall_sec` exceeds the threshold. This is a recovery-design hook for multi-second RX tails, not a promoted default; the next hardware run must prove whether it lowers p95 without creating extra retries.
- 2026-07-11 Windows Cockpit resume validation: the immediate all-fallback failure after backend restart was traced to Git Bash/Windows sshpass losing binary stdin during IQ remote-decode asset upload (`getsockname failed: Not a socket`). A direct stdin test failed on the default helper path and passed with `OPENAMP_SSH_RUNNER=docker`, so Cockpit Desktop now injects Docker SSH defaults on Windows. Smoke `batch-1783748751-50` stayed `50/50`, fallback `0`, with TVM median/p95 `242.02/244.31 ms` and IQ median/p95 `176.61/320.35 ms`.
- 2026-07-11 RX health/session A/B after the Docker SSH fix: `batch-1783748855-300` (health reset on, batch session window `16`) stayed `300/300`, fallback `0`, with TVM `240.72/243.50 ms` median/p95 and IQ `161.95/289.82 ms`; `rx_health_reset_count=5`, `>500 ms=8`. Turning health reset off (`batch-1783749340-300`) worsened IQ p95 to `525.65 ms`, `>500 ms=16`. Keeping health reset on but making the RX session whole-batch (`ANALOG_RX_BATCH_SESSION_MAX_IMAGES=0`, `batch-1783749717-300`) worsened IQ p95 to `556.29 ms`, `>500 ms=18`. Current recommendation: keep Docker SSH, health reset on with threshold `0.25 s`, and the existing batch window `16`; do not promote whole-batch RX session.
- 2026-07-11 current best versus target: IQ median remains far below TVM median (`161.95 ms` versus `240.72 ms`), but IQ p95 is still above TVM p95 (`289.82 ms` versus `243.50 ms`). This is a recovered, demo-usable Cockpit path, not the final p95 target. Remaining tails are mostly RX/server capture stalls and occasional decode-worker waits; QPSK remains frozen.
- 2026-07-11 next-resume hypothesis order: do not change QPSK or the accepted IQ default while paused. When hardware runs resume, first isolate RX control-plane scheduling by comparing the accepted profile against the same profile with direct non-Tailscale RX control host if available, or with only RX control command timing instrumentation enabled. Second, A/B soft-completion thresholds between `0.035` and `0.05` only after RX control tails are stable. Third, revisit publish-event or worker response protocol only if it improves a 50-image smoke without increasing RX tails. Do not retest `/tmp`, batch window `32`, or no-batch unless new evidence contradicts the audit above.
- 2026-07-11 publish-event diagnostic: `ANALOG_REMOTE_DECODE_PUBLISH_EVENT=1` makes the board decode-server emit a lightweight `published` event immediately after `atomic_savez`, and the runner can treat that event as soft completion without path-probe blocking. Local tests cover request gating, worker event matching, and partial decode timing in the publish payload. Hardware evidence is mixed: `batch-1783732029-20` stayed `20/20`, fallback `0`, with IQ p95 `236.94 ms` below TVM p95 `259.66 ms`; but the follow-up `batch-1783732287-50` stayed `50/50`, fallback `0`, with IQ p95 `363.37 ms` above TVM p95 `247.02 ms`. Do not promote this switch to the default profile; keep it opt-in for diagnosing stdout latency versus true board decode/RX tails.
- 2026-07-11 low-sync early-retry A/B: `ANALOG_RETRY_ON_LOW_SYNC=1` with `ANALOG_LOW_SYNC_RETRY_THRESHOLD=0.06` stayed all-pass in `batch-1783732885-50`, but IQ p95/max was `273.66/518.02 ms` versus TVM p95 `243.78 ms`. This avoids some weak-sync decode cost but does not solve RX capture/wait or worker-wait tails, so it remains opt-in.
- 2026-07-11 decode-worker timing / soft-probe timeout fix validation: `batch-1783726777-300` stayed `300/300`, fallback `0`; TVM median/p95/max was `241.78/244.73/277.54 ms`; IQ total median/p95/max was `158.38/333.24/10395.30 ms`; PSNR was `37.0445`, SSIM was `0.97494`. The new worker timing metrics showed `remote_decode_worker_response_wait_ms` median/p95/max `63.50/132.16/405.91`, down from the previous `8694 ms` max class. This proves the soft-completion path-probe timeout fix worked, but it does not solve the remaining RX arm/capture tail (`rx_capture_ms` p95/max `208.24/10317.99`).
- 2026-07-11 rejected RX WAIT timeout A/B: `batch-1783727897-300` used `ANALOG_RX_WAIT_TIMEOUT_SEC=1.0` and stayed `300/300`, fallback `0`, but IQ total p95/max worsened to `904.15/10540.47 ms`. The value was present in every round record, so the parameter was correctly plumbed; the regression came from not-armed / WAIT-timeout recovery and 8-second STOP-drain tails. Keep `ANALOG_RX_WAIT_TIMEOUT_SEC` opt-in only.
- 2026-07-11 rejected short-STOP A/B: `batch-1783728580-300` used `ANALOG_RX_WAIT_TIMEOUT_SEC=1.0` plus `RX_STOP_WAIT_MS=200`, `ANALOG_RX_STOP_DRAIN_TIMEOUT_SEC=0.2`, and `ANALOG_RX_STOP_ARM_FAIL_TIMEOUT_SEC=0.0`; it failed the all-pass gate (`296/300`, fallback `4`). `batch-1783728902-300` removed `ANALOG_RX_WAIT_TIMEOUT_SEC` but kept short STOP and still failed (`299/300`, fallback `1`). Short STOP lowers some wait samples but can leave the RX server in `capture_already_running`; keep the stable default `RX_STOP_WAIT_MS=8000` / `ANALOG_RX_STOP_DRAIN_TIMEOUT_SEC=8.0`.
- 2026-07-11 code-only follow-up from these A/B runs: Cockpit now passes `ANALOG_RX_WAIT_TIMEOUT_SEC` through to `RunAnalogLatentBatch.py` as `--rx-wait-timeout-sec`, and also preserves opt-in `ANALOG_RX_STOP_ARM_FAIL_TIMEOUT_SEC`. Neither is a default performance profile until a 300-image all-pass run proves it.
- 2026-07-11 valid rerun after path-probe request matching and Windows Docker-TX default fix: `batch-1783724189-300` stayed `300/300`, fallback `0`; TVM `input_count=300`, `available_input_count=300`, `processed_count=300`; TVM median/p95/max was `241.67/244.62/258.74 ms`; IQ total transport median/mean/p95/max was `156.24/240.41/274.60/7717.03 ms`; PSNR was `37.0445`, SSIM was `0.97494`. The 20-image smoke immediately before it, `batch-1783724140-20`, stayed `20/20`, fallback `0`, with TVM median/p95 `241.22/253.07 ms` and IQ median/p95 `158.12/168.73 ms`. This fixes the invalid 299-input TVM failure but still misses the final p95 target.
- 2026-07-11 invalid 300-image run root cause: `batch-1783722161-300` had transport summary `300/300`, but the board directory contained only `299` decoded files and missed `00000244.npy`; TVM raw progress also skipped `00000244.npy`, then timed out at `299/300`. Root cause was `RemotePathProbeWorker.probe()` accepting a stale `ok` response without checking `request_id`. It now requires matching `request_id`; local coverage is `test_remote_path_probe_worker_ignores_stale_response_for_previous_request`, and `USRP292x/test_analog_latent_link.py` passed.
- Windows native backend fix: TX startup now defaults to Docker on Windows when Docker is available, unless `OPENAMP_USRP_TX_RUNNER=local/host/bash` is explicit. This avoids the Windows symlink/reparse-point case where `OtaTxPersistentServer` exists for Python but fails Git Bash `[[ -x ]]`. Covered by `test_usrp_tx_server_defaults_to_docker_on_windows_when_available`.
- 2026-07-11 current live candidate after repeated soft completion, Cockpit default session fixes, `RX_ARM_WAIT_MS=150`, and short not-armed STOP: `batch-1783719194-300` stayed `300/300`, fallback `0`; TVM median/p95 was `241.00/243.32 ms`; Cockpit API IQ median/mean/p95/max was `158.27/226.27/266.12/10092.53 ms`; stage summary IQ total p95 was `274.86 ms`; PSNR was `37.0445`, SSIM was `0.97494`. This is better than the rejected `64`-window A/B but still misses the final target because IQ p95 remains above TVM p95 by roughly `23 ms`.
- 2026-07-11 default-profile restart smoke: `batch-1783720370-20` stayed `20/20`, fallback `0`, with IQ total median/mean/p95/max `163.59/178.41/249.12/268.53 ms`, PSNR `37.0445`, and SSIM `0.97494`. `rx_server_arm_wait_ms` p95 stayed below `1 ms`, proving Cockpit now passes the intended `RX_ARM_WAIT_MS=150` path instead of the old 2-second RX arm wait.
- 2026-07-11 rejected 300-image rerun after short-STOP escalation: `batch-1783721600-300` failed the gate (`299/300`, fallback `1`). Image 288 hit two not-armed attempts and then `RX_metadata_error: Unknown error code 0x10`; IQ p95 was not valid for promotion. This run also exposed a profile mismatch: `rx_post_quantize=True` despite the recommendation to disable RX post-quantization. Cockpit defaults now set `ANALOG_RX_POST_QUANTIZE=0`, covered by `test_board_access_usrp_iq_defaults_fast_rx_arm_status_timeout`; rerun hardware after restart before drawing performance conclusions.
- Rejected/invalid A/B after the default fixes: `ANALOG_RX_BATCH_SESSION_MAX_IMAGES=64` stayed all-pass but worsened 300-image p95/max and is rejected; `ANALOG_REMOTE_DECODE_SOFT_COMPLETE_SEC=0.02` failed before transport because the remote decode worker SSH startup hit `kex_exchange_identification`, so it is not a valid performance result. Current soft-completion default is `0.05` with persistent probe repeated checks.
- Current local-only fix awaiting hardware validation: not-armed recovery now sends the short `STOP timeout=<ANALOG_RX_STOP_ARM_FAIL_TIMEOUT_SEC>` first, but if the final RX state is still `busy=1`, it escalates to the full STOP drain before ARQ retry. `rx_control_response_busy()` now uses the latest `busy=` token so a STOP response followed by idle STATUS is not misclassified. Local RED/GREEN coverage: `test_rx_control_response_busy_uses_latest_busy_token` and `test_process_image_escalates_arm_failure_stop_when_rx_remains_busy`.
- 2026-07-11 live candidate after the `.npy`/mmap pass: `batch-1783713085-300` used Docker SSH/TX, board `/home/user/venv/bin/python`, `REMOTE_RX_RUN_ROOT=/tmp/usrp292x_remote_runs`, `ANALOG_RX_TAIL_SEC=0.040`, `ANALOG_REMOTE_DECODED_FORMAT=npy`, `ANALOG_RX_SC16_MMAP=1`, `ANALOG_RX_CLIPPING_DECIMATION=8`, bounded RX batch sessions at `16`, minimal decode responses, response-only summary, and precreated capture dirs. It stayed `300/300`, fallback `0`; TVM median/mean/p95/max was `241.45/242.16/244.54/261.01 ms`; IQ total transport median/mean/p95/max was `155.23/240.64/271.00/9773.22 ms`; PSNR was `37.0445`, SSIM was `0.97494`. All remote decoded latent files were `.npy`. `read_sc16` p95 dropped to `2.89 ms`, and decoded-output write p95 was `1.42 ms`, but rare write/decode/RX-capture tails still keep IQ p95 above TVM p95. Treat this as the current best live candidate, not final p95 success.
- Cockpit state fix after `batch-1783713085-300`: the terminal USRP wrapper summary now carries the final `iq_remote_decode_manifest`, and `/api/batch-state` prefers final `.npy` files over early `.npz` manifest defaults. Verification smoke `batch-1783713778-5` returned five `done` images with `/home/user/cockpit_usrp_rx/.../*.npy` paths, TVM median `239.49 ms`, max `254.86 ms`, PSNR `37.0445`, and SSIM `0.97494`.
- Latest accepted partial improvement: the IQ runner now pre-creates every remote capture directory before the first `CAPTURE`, moving rare per-image `create_directories()` stalls out of the RX hot path. The Cockpit-equivalent 50-image smoke `batch-1783706692-50` stayed `50/50`, fallback `0`, with `remote_capture_dirs_precreated=true`, TVM median/p95/max `240.74/243.98/256.89 ms`, IQ median/p95/max `148.70/207.52/301.42 ms`, PSNR `37.0445`, and SSIM `0.97494`. The matching 300-image gate `batch-1783707048-300` stayed `300/300`, fallback `0`, `remote_capture_dirs_precreated=true`, TVM median/p95/max `241.49/244.48/287.26 ms`, IQ median/p95/max `149.14/279.76/3831.98 ms`, PSNR `37.0445`, and SSIM `0.97494`. Treat this as a reliability/tail reduction improvement, not final success: IQ p95 still exceeds TVM p95 on 300 images.
- Previous pre-mmap recommended profile: fast-first sync, `ANALOG_RX_POST_QUANTIZE=0`, remote-dir `.npz`, `ANALOG_PIPELINE_DEPTH=1`, `RX_ARM_WAIT_MS=150`, `ANALOG_RX_WAIT_TIMEOUT_SEC=1.0`, `ANALOG_RX_STOP_DRAIN_TIMEOUT_SEC=8.0`, `PERSISTENT_RX_TX_DELAY=0.0`, `ANALOG_PRECONNECT_CONTROL=1`, `ANALOG_RX_SESSION_CONTROL=1`, `ANALOG_RX_BATCH_SESSION_CONTROL=1`, `ANALOG_RX_BATCH_SESSION_MAX_IMAGES=16`, `ANALOG_PRECONNECT_RX_CAPTURE_CONTROL=0`, `ANALOG_REMOTE_DECODE_RESPONSE_MODE=minimal`, `ANALOG_REMOTE_DECODE_RESPONSE_ONLY_SUMMARY=1`, `ANALOG_PRECREATE_REMOTE_CAPTURE_DIRS=1`, and `REMOTE_RX_RUN_ROOT=/tmp/usrp292x_remote_runs`.
- Latest rejected A/B runs: moving capture files to `/dev/shm/usrp292x_remote_runs` passed 50 images (`batch-1783707439-50`) but worsened IQ p95 to `264.79 ms` due RX capture/control tails; shrinking `ANALOG_RX_BATCH_SESSION_MAX_IMAGES` to `8` passed 50 images (`batch-1783707616-50`) but worsened IQ median/p95/max to `175.06/363.43/655.65 ms`. Keep `/tmp` and batch-session window `16`.
- Latest container-migration validation: `batch-1783671744-300` used the Cockpit API path with Docker TX, board RX under `/home/user/venv`, local latent cache `E:\Main\Career\集创赛\jscc-test\encoder_outputs`, IQ direct, handwritten TVM, and big.LITTLE. It stayed `300/300`, fallback `0`; TVM median `241.04 ms`, mean `241.75 ms`, p95 `243.75 ms`, max `280.63 ms`; IQ transport median `171.54 ms`, mean `232.19 ms`, p95 `363.50 ms`, max `5543.82 ms`. This confirms the container/Cockpit path has recovered the expected near-250ms visible reconstruction speed.
- Latest container-migration stage timing: `tx_control_ms` median `23.95`, p95 `30.96`; `rx_arm_ms` median `24.73`, p95 `102.53`; `rx_capture_ms` median `88.60`, p95 `199.69`; `rx_wait_ms` median `40.04`, p95 `51.87`; remote decode median `60.89`, p95 `158.07`; board-reported decode median `43.58`, p95 `48.77`. The normal path is stable, but `305` stage records for `300` images show `5` recovered extra attempts; the remaining target is the rare multi-second RX arm/capture tail.
- Current candidate IQ direct profile: fast-first sync, `ANALOG_RX_POST_QUANTIZE=0`, remote-dir `.npz`, `ANALOG_PIPELINE_DEPTH=1`, `RX_ARM_WAIT_MS=150`, `ANALOG_RX_ARM_STATUS_TIMEOUT_SEC=0.5`, `ANALOG_RX_ARM_STATUS_POLL_SEC=0.025`, `ANALOG_RX_WAIT_TIMEOUT_SEC=1.0`, `ANALOG_RX_WAIT_CONTROL_TIMEOUT_MARGIN_SEC=1.0`, `ANALOG_RX_STOP_DRAIN_TIMEOUT_SEC=8.0`, `PERSISTENT_RX_TX_DELAY=0.0`, `ANALOG_PRECONNECT_CONTROL=1`, `ANALOG_RX_SESSION_CONTROL=1`, `ANALOG_PRECONNECT_RX_CAPTURE_CONTROL=0`, `ANALOG_REMOTE_DECODE_RESPONSE_MODE=minimal`, latest gate `batch-1783674397-300`, `300/300`, fallback `0`, all-pass `true`.
- Current candidate transport with `RX_ARM_WAIT_MS=150`: 50-image smoke `batch-1783672570-50` stayed `50/50`, fallback `0`, TVM median `241.58 ms`, IQ transport median `169.31 ms`, p95 `245.63 ms`, max `321.96 ms`, and produced no extra retry records. The 300-image gate `batch-1783672642-300` stayed `300/300`, fallback `0`, TVM median `241.93 ms`, p95 `248.18 ms`, IQ transport median `170.84 ms`, p95 `332.98 ms`, max `6057.65 ms`, and still produced `305` transport stage records for `300` images. After the session-close shutdown fix, `batch-1783674397-300` stayed `300/300`, fallback `0`, TVM median `238.87 ms`, p95 `245.54 ms`, IQ transport median `175.28 ms`, p95 `337.46 ms`, and still produced `305` stage records, so the fix is safe but not a 300-image tail cure.
- Current stable transport: cockpit-button-equivalent 300-image run `batch-1783668000-300` had IQ transport median `169.42 ms`, p95 `304.21 ms`, mean `250.74 ms/image`, max `7354.09 ms`. It produced `303` round records because `3` records were recovered by ARQ retry. This is now the recommended Cockpit IQ speed profile until the remaining capture/decode max outliers are removed.
- Current stable stage timing: `tx_control_ms` median `25.60`, p95 `35.05`; `rx_arm_ms` median `19.52`, p95 `46.46`; `rx_capture_ms` median `83.71`, p95 `131.62`; `rx_wait_ms` median `38.45`, p95 `46.71`; remote decode median `62.98`, p95 `140.47`; board-reported decode median `44.79`, p95 `94.66`. Same-session RX control keeps normal capture and wait control overhead bounded while preserving the one-click Cockpit path.
- Current stable TVM/big.LITTLE result: processed `300/300`, median `241.21 ms`, mean `241.69 ms`, min `240.21 ms`, p95 `243.94 ms`, max `254.83 ms`, `artifact_sha256_match=true`.
- Current stable long-tail caveat: the final profile still has rare recovered tails. In `batch-1783668000-300`, `image 145` retried once after low sync metric, and `image 263` retried after `RX CAPTURE did not arm before TX` followed by one `capture_already_running`. The 8-second STOP drain prevented a batch fallback, but the max remains multi-second. The next fix should make RX STOP/cancel server-side deterministic before retry.
- Current stability fix set: RX server `CAPTURE` arm wait and `STOP` drain wait are configurable (`--arm-wait-ms`, `--stop-wait-ms`), Cockpit/USRP startup preserves IQ timing and Docker runner env vars, the Python runner records `rx_arm_status_timeout_sec` and `rx_capture_preconnect_enabled`, Docker TX respects `OPENAMP_USRP_TX_DOCKER_MOUNT_TARGET`, and session-control error handling closes the RX session before issuing a server-side wait-on-STOP.
- Previous zero-delay profile without preconnect: batch `batch-1783655994-300`, `300/300`, fail `0`, transport median `171.31 ms`, p95 `300.72 ms`, wall `78.466 s`; TVM median `242.066 ms`, wall `76.437 s`.
- Previous depth-1 stability profile before zero TX delay: batch `batch-1783655547-300`, `300/300`, fail `0`, transport median `183.53 ms`, p95 `333.82 ms`, max `7987.46 ms`; TVM median `241.472 ms`, wall `76.758 s`, `255.859 ms/image`.
- Preconnect repeatability evidence: batch `batch-1783656629-300` stayed `300/300` with no retry/error records and improved transport median/p95 to `167.36 ms` / `296.90 ms`, but mean/wall worsened to `243.58 ms/image` / `80.150 s` because three decode-worker response waits lasted `3.0-5.5 s` while board-reported decode stayed `40-56 ms`. Repeat batch `batch-1783658102-300` kept `300/300` and improved median/p95/wall again, so preconnect is now recommended for the Cockpit IQ speed profile, while decode/RX tails remain open.
- Previous depth-2 post-quantize-off profile: fast-first sync, `ANALOG_RX_POST_QUANTIZE=0`, batch `batch-1783638234-300`, `300/300`, fail `0`, fallback `0`, transport median `186.83 ms`, p95 `351.67 ms`, TVM median `241.28 ms`, p95 `244.10 ms`, wall `76.12 s`.
- Depth 2 is no longer the default recommendation. Follow-up runs after the RX fixes (`batch-1783648476-300`, `batch-1783649095-300`, `batch-1783649536-300`, `batch-1783650496-300`) still showed `started=0`, capture-busy, and arm-timeout chains when board-side decode/write overlapped the next RX arm.
- Previous fast-first profile with post-quantize on: batch `batch-1783626884-300`, `300/300`, fail `0`, fallback `0`, transport median `182.28 ms`, p95 `372.96 ms`, TVM median `242.41 ms`, p95 `245.77 ms`, wall `157.12 s`.
- End-to-end cockpit API wall for the earlier post-quantize-off 300-image run was `169.115 s`. Transport wall was `59.77 s`; that profile is retained as historical evidence, not the current stable default.
- Optional `.npy` remote-dir profile: `ANALOG_REMOTE_DECODED_FORMAT=npy` publishes decoded latent files as native NumPy arrays. `/home/user/cockpit_usrp_rx` run `batch-1783640049-300` stayed `300/300`, fallback `0`, transport median `189.19 ms`, p95 `383.88 ms`, TVM median `241.49 ms`, p95 `248.64 ms`, and write p95 `1.64 ms`, but one `/home` filesystem stall still made write max `3337.46 ms`.
- Optional tmpfs decoded-output profile: board access `remote_usrp_rx_dir=/dev/shm/cockpit_usrp_rx` plus `.npy`. Run `batch-1783640401-300` stayed `300/300`, fallback `0`, TVM median `242.11 ms`, p95 `244.97 ms`, write median `0.827 ms`, p95 `1.54 ms`, max `3.43 ms`, but transport median `187.09 ms` and p95 `1013.67 ms` because RF/RX no-sync and control stalls dominated. This is not a default until RX health is stabilized.
- Optional promoted-profile tmpfs diagnostic: board access `remote_usrp_rx_dir=/dev/shm/cockpit_usrp_rx` with `.npz + preconnect + zero TX delay` capped decoded-output writes (`write_npz` about `1.4-3.1 ms` in the 50-image smoke and about `1.4-1.9 ms` in top 300-image tail records). However, the 300-image run `batch-1783659032-300` worsened transport to median `170.03 ms`, p95 `285.40 ms`, max `8705.21 ms`, wall `78.550 s`; TVM still processed `300/300`, median `239.970 ms`, wall `75.311 s`, `251.037 ms/image`, artifact SHA matched. The new worst tails were late decode-worker stdout/SSH waits where board-reported decode stayed about `39-58 ms`, so tmpfs is an I/O diagnostic, not the default.
- Optional tmpfs plus soft-completion diagnostic: `batch-1783659430-300` used `/dev/shm/cockpit_usrp_rx`, `.npz`, preconnect, zero TX delay, and `ANALOG_REMOTE_DECODE_SOFT_COMPLETE_SEC=0.25`. It stayed `300/300`, but transport worsened to median `167.38 ms`, p95 `355.63 ms`, max `7401.60 ms`, wall `82.673 s`; TVM processed `300/300`, median `240.951 ms`, wall `75.650 s`, `252.165 ms/image`, artifact SHA matched. Soft completion triggered only partially (`4` checks, `2` non-empty) and did not reliably cap late stdout waits, so leave it disabled by default.
- Optional minimal decode-worker response profile: `ANALOG_REMOTE_DECODE_RESPONSE_MODE=minimal` keeps the full `decode_summary.json` on the board but returns only runner-required summary fields over the persistent worker stdout path. This avoids sending the large full decode summary through SSH stdout and replaces per-image soft-completion SSH probes with a same-worker protocol change.
- Minimal response on `/home`: 50-image smoke `batch-1783661396-50` stayed `50/50`, fallback `0`, remote decode logs were only `728-735 bytes`, and the top tails were RX wait/arm rather than stdout response. The 300-image gate `batch-1783661701-300` stayed `300/300`, recovered two low-sync records, transport wall `72.983 s`, mean `217.71 ms/image`, median `168.01 ms`, p95 `284.14 ms`, max `3566.01 ms`; TVM processed `300/300`, median `242.443 ms`, wall `76.830 s`, `256.101 ms/image`, artifact SHA matched. Remote decode p95 improved to `84.75 ms`, but one true board decode/write stall remained (`image 179`, board-reported `3.419 s`) and one runner-side response wait remained (`image 163`, runner `1.516 s` vs board `40 ms`).
- Minimal response on tmpfs: `batch-1783662016-300`, `/dev/shm/cockpit_usrp_rx`, stayed `300/300`, recovered three low-sync records, transport wall `74.485 s`, mean `223.06 ms/image`, median `167.60 ms`, p95 `313.20 ms`, max `5347.40 ms`; TVM processed `300/300`, median `241.566 ms`, wall `83.150 s`, `277.166 ms/image`, artifact SHA matched. This capped board-reported decode max at `65.26 ms` and worker decode max at `681.37 ms`, but RX arm/capture produced the worst tails (`image 198`, `rx_arm_ms=5229.40`, `rx_capture_ms=5270.52`). Do not promote tmpfs+minimal as default; the next bottleneck is RX arm/capture health.
- Previous sequential-sync IQ baseline: batch `batch-1783625337-300`, `300/300`, fail `0`, fallback `0`, transport median `189.10 ms`, p95 `642.66 ms`, board decode median `68.47 ms`, TVM median `242.16 ms`, wall `161.17 s`.
- Streaming-TV-overlap experiment: batch `batch-1783624303-300`, `300/300`, fail `0`, fallback `0`, but total wall worsened to `233.76 s`; transport median `338.90 ms`, p95 `694.20 ms`; TVM median `248.29 ms`, p95 `322.37 ms`.
- Earlier IQ direct baseline: batch `batch-1783610422-300`, transport median `202.54 ms`, p95 `598.89 ms`, TVM median `241.21 ms`.
- QPSK baseline: batch `batch-1783610673-300`, `300/300`, fail `0`; transport `2961.78 ms/image`, RF airtime mean `48.02 ms`, decode command mean `2295.96 ms`, TVM median `240.06 ms`.
- Security state during performance runs: `channel_state=disabled`, `auth_enabled=true`, `sig_policy=DUAL_REQUIRED`, `board_configured=true`.

The target is to preserve the visible Cockpit reconstruction result and move IQ direct toward a stable sub-250ms display path. The current stable profile meets the 300-image all-pass gate and keeps IQ transport median far below the TVM median, while TVM still dominates the visible reconstruction cadence at about `255 ms/image`. The next optimization should reduce the remaining IQ mean/tail and decode/control overhead before re-enabling any unsafe overlap.

## 2026-07-10 Execution Update

- Late-evening status calibration:
  - `batch-1783691290-300` remains the latest no-batch 300-image reference for the current stable profile: `300/300`, fallback `0`, TVM median/p95/max `241.84/253.13/265.85 ms`, IQ transport median/p95/max `172.71/301.74/8598.55 ms`. The median target is met; p95/max are not.
  - `batch-1783694251-50` is the latest Cockpit-button-equivalent 50-image probe after adding tail-split diagnostics: `50/50`, fallback `0`, TVM median/p95/max `241.53/247.05/257.82 ms`, IQ transport median/p95/max `217.35/520.90/1306.86 ms`.
  - New derived diagnostics now split RX control tails into `rx_arm_control_overhead_ms`, `rx_post_arm_to_wait_ms`, and `rx_wait_response_overhead_ms`. In `batch-1783694251-50`, `rx_server_receive_ms` stayed tight at median/p95/max `63.61/63.64/64.03 ms`, while `rx_arm_control_overhead_ms` hit max `1159.40 ms` and `rx_wait_response_overhead_ms` p95/max reached `125.83/266.76 ms`. This points the next optimization at runner/control-response boundaries, not the steady receive loop.
  - Cockpit visibility update: `/api/crypto-status` now carries `batch_iq_stage_benchmark`, and the Cockpit benchmark table shows RX arm/capture/wait plus the new arm-control, WAIT-response, and decode-response tail rows alongside transport and TVM reconstruction metrics.
  - RX arm split validation `batch-1783695696-50`: after adding `rx_session_open_ms` and `rx_capture_command_ms`, the Cockpit-equivalent 50-image run stayed `50/50`, fallback `0`. TVM median/p95/max was `242.85/259.12/370.31 ms`; IQ median/p95/max was `173.20/335.37/392.09 ms`. `rx_session_open_ms` median/p95/max was `17.52/33.38/38.52 ms`; `rx_capture_command_ms` was `9.03/17.88/101.49 ms`; `rx_wait_response_overhead_ms` p95 dropped to `9.12 ms`, while `remote_decode_response_overhead_ms` p95 remained `125.61 ms`. This suggests the next fix should target decode response/board decode tails first, while keeping an eye on rare CAPTURE command response spikes.
  - Response-only-summary A/B `batch-1783696115-50`: `ANALOG_REMOTE_DECODE_RESPONSE_ONLY_SUMMARY=1` stayed `50/50`, fallback `0`, but did not improve the decision metric. TVM median/p95/max was `242.78/266.33/298.52 ms`; IQ median/p95/max was `171.53/334.85/1166.81 ms`. `remote_decode_response_overhead_ms` improved only modestly to median/p95/max `14.77/113.42/126.89 ms`, while a new `rx_session_open_ms` max `1015.27 ms` dominated the run. Keep `ANALOG_REMOTE_DECODE_RESPONSE_ONLY_SUMMARY=0` as the default.
  - Bounded RX batch session A/B `batch-1783696822-50`: added opt-in `ANALOG_RX_BATCH_SESSION_MAX_IMAGES=16` on top of `ANALOG_RX_BATCH_SESSION_CONTROL=1`. The run stayed `50/50`, fallback `0`; TVM median/p95/max was `241.94/246.45/275.62 ms`; IQ median/p95/max was `154.18/326.68/333.40 ms`. `rx_session_open_ms` was `0` because the shared session was reused inside each window, `rx_capture_command_ms` median/p95 was `9.16/18.02 ms`, and `rx_wait_response_overhead_ms` p95 was `3.65 ms`. This is a useful opt-in A/B because it lowers median sharply, but it is not the default until a 300-image run proves p95 below the no-batch gate.
  - Combined bounded-session plus response-only-summary A/B: `batch-1783697847-50` stayed `50/50`, fallback `0`; TVM median/p95/max was `239.25/243.52/257.93 ms`, and IQ median/p95/max was `153.55/237.32/286.80 ms`, the first current-line 50-image run where IQ p95 beat TVM p95. The 300-image gate `batch-1783697942-300` stayed `300/300`, fallback `0`; TVM median/p95/max was `239.99/243.44/296.38 ms`, and IQ median/p95/max was `153.37/269.84/8378.85 ms`. This is the best current candidate versus no-batch p95 `301.74 ms` and batch-session-only p95 `321.68 ms`, but p95 and max still miss the final "IQ comfortably below TVM" target.
  - Output-only soft-completion diagnostic: with `ANALOG_REMOTE_DECODE_SOFT_COMPLETE_SEC=0.14`, `batch-1783698593-50` stayed `50/50`, fallback `0`, but IQ median/p95/max worsened to `169.39/365.51/2122.73 ms` and `soft_count=0`. The board output was not visible at the soft deadline during the outliers, so this points to request scheduling/worker response latency rather than only late stdout after a completed NPZ. Do not promote the old `0.14` setting.
  - Whole-batch RX session reuse rejection: `ANALOG_RX_BATCH_SESSION_MAX_IMAGES=0` plus response-only-summary stayed `50/50` in `batch-1783700070-50`, but IQ median/p95/max was `185.69/571.83/1217.93 ms`. The p95 moved to `rx_session_open_ms`, `rx_arm_ms`, and RX control overhead, so keep the bounded window at `16` rather than a whole-batch shared session.
  - RX CAPTURE preconnect rejection on the combined profile: `ANALOG_PRECONNECT_RX_CAPTURE_CONTROL=1` with max-images `16` stayed `50/50` in `batch-1783700726-50`, and it did reduce RX control p95 (`rx_capture_control_overhead_ms` p95 `20.81 ms`, `rx_wait_response_overhead_ms` p95 `4.91 ms`). Overall IQ median/p95/max was still `163.70/270.40/331.97 ms`, worse than the no-preconnect 50-image candidate, because the remaining tail shifted to board decode. Keep `ANALOG_PRECONNECT_RX_CAPTURE_CONTROL=0`.
  - Remote decode worker CPU-affinity diagnostic: added opt-in `ANALOG_REMOTE_DECODE_WORKER_PREFIX` so live runs can launch the board decode worker through a prefix such as `taskset -c 2`. With bounded batch session plus response-only-summary, `batch-1783701414-50` stayed `50/50`, fallback `0`, TVM median/p95/max `246.16/322.50/377.92 ms`, quality PSNR `37.0445`, SSIM `0.97494`, and IQ median/p95/max `154.96/177.68/254.72 ms`. The 300-image gate `batch-1783701712-300` stayed `300/300`, fallback `0`, TVM median/p95/max `239.15/242.35/273.96 ms`, PSNR `37.0445`, SSIM `0.97494`, and IQ median/p95/max `151.66/275.68/8884.26 ms`. Conclusion: taskset is useful diagnostics and improves the 50-image p95, but it does not beat the previous 300-image candidate p95 `269.84 ms` and does not clear the "IQ p95 below TVM p95" target.
  - Soft-completion repeat on the taskset profile: `ANALOG_REMOTE_DECODE_SOFT_COMPLETE_SEC=0.14` in `batch-1783702255-300` did not trigger soft completion (`soft_count=0`) and failed the gate (`298/300`, fail `2`). IQ p95 was low (`~189 ms` in the runner summary) only because two RX failures were excluded from passing transport records; the failed frames were RX metadata / not-armed failures, not TVM or decode output quality. Do not promote the old `0.14` setting.
  - RX arm status recovery fix: `wait_for_rx_capture_armed` now allows direct `STATUS` fallback when a shared/session `STATUS` command times out after being sent. This mirrors the existing `WAIT` fallback path and avoids treating a stale shared socket as proof that RX never armed. Local RED/GREEN coverage is `test_wait_for_rx_capture_armed_falls_back_after_sent_session_status_timeout`.
  - Recovery-fix smoke: `batch-1783703143-50` stayed `50/50`, fallback `0`, TVM median/p95/max `240.75/246.59/262.73 ms`, IQ median/p95/max `155.08/224.53/306.85 ms`, PSNR `37.0445`, SSIM `0.97494`. This confirms no short-run regression.
  - Recovery-fix 300-image gate: `batch-1783703433-300` stayed `300/300`, fallback `0`, TVM median/p95/max `240.23/243.72/259.31 ms`, IQ median/p95/max `150.61/285.72/8847.20 ms`, PSNR `37.0445`, SSIM `0.97494`, stage records `302`. Treat this as a reliability fix, not a promoted performance profile: IQ p95 is worse than the prior best `269.84 ms` and remains above TVM p95. The top tails were true board `.npz` write stalls (`write_npz` up to `5526.98 ms`), runner-side decode response waits where board-reported decode stayed near `39-45 ms`, and one recovered `RX CAPTURE did not arm before TX` attempt whose retry succeeded.
  - Tmpfs decoded-output repeat: `REMOTE_USRP_RX_DIR=/dev/shm/cockpit_usrp_rx` in `batch-1783704246-50` stayed `50/50`, fallback `0`, and capped `write_npz` p95/max at `2.55/2.61 ms`, but IQ median/p95/max worsened to `153.18/508.96/567.11 ms` because RX capture/wait tails dominated. Keep tmpfs diagnostic-only.
  - Summaryless soft-completion fix: response-only-summary mode no longer asks soft completion to wait for a remote `decode_summary.json` that the request intentionally does not write, and server startup now passes through `ANALOG_REMOTE_DECODE_RESPONSE_ONLY_SUMMARY` plus `ANALOG_REMOTE_DECODE_SOFT_COMPLETE_SEC`. The 50-image A/B `batch-1783704792-50` stayed `50/50`, fallback `0`, TVM `240.29/246.72/258.10 ms`, IQ `154.63/219.79/278.24 ms`, PSNR `37.0445`, SSIM `0.97494`; no soft completion fired because worker responses were already below the soft threshold.
  - Soft-completion 300-image gate rejection: `batch-1783704920-300` fired `5` soft completions but failed the all-pass gate (`299/300`, fallback `1`) after a RX WAIT/not-armed chain. IQ median/p95/max over passed transport records was `152.19/247.85/13838.31 ms`, but this is not valid promotion evidence because one image failed and the soft-completed records still cost about `2 s` due the SSH file probe. Keep `ANALOG_REMOTE_DECODE_SOFT_COMPLETE_SEC=0` by default.
  - Remote capture directory precreation fix: root-cause evidence from rejected `batch-1783704920-300` showed not-armed jobs with multi-second `wall_sec` but zero `drain_sec`, `stream_cmd_sec`, and `receive_sec`, which points to pre-capture filesystem preparation (`create_directories` / file open) rather than UHD streaming. `RunAnalogLatentBatch.py` now pre-creates all per-image remote capture directories once per batch, before the first `CAPTURE`, behind `ANALOG_PRECREATE_REMOTE_CAPTURE_DIRS=1` (default on). Local RED/GREEN coverage is `test_batch_runner_remote_decode_precreates_capture_dirs_before_first_capture`; server and Docker wrappers pass through `ANALOG_PRECREATE_REMOTE_CAPTURE_DIRS` and `ANALOG_PRECREATE_REMOTE_CAPTURE_DIRS_CHUNK`.
  - Precreate 50-image smoke: `batch-1783706692-50` stayed `50/50`, fallback `0`, `remote_capture_dirs_precreated=true`, TVM median/p95/max `240.74/243.98/256.89 ms`, IQ median/p95/max `148.70/207.52/301.42 ms`, PSNR `37.0445`, SSIM `0.97494`, `rx_batch_session_max_images=16`, and response-only-summary enabled. This is a strong short-run improvement: IQ p95 beat TVM p95 by about `36 ms`.
  - Precreate 300-image gate: `batch-1783707048-300` stayed `300/300`, fallback `0`, `remote_capture_dirs_precreated=true`, TVM median/p95/max `241.49/244.48/287.26 ms`, IQ median/p95/max `149.14/279.76/3831.98 ms`, PSNR `37.0445`, SSIM `0.97494`, and `303` stage records. This improves reliability and caps the previous multi-second directory/`.npz` write class (`write_npz` p95/max `2.09/21.42 ms`) but still misses the final p95 target. Remaining top classes are board `read_sc16` stalls (`image 201`, `read_sc16=3667 ms`), RX control/capture response tails (`image 71/213/210/222/78`), and three recovered retries.
  - Rejected capture-root tmpfs A/B: `REMOTE_RX_RUN_ROOT=/dev/shm/usrp292x_remote_runs` passed 50 images in `batch-1783707439-50`, fallback `0`, but worsened IQ median/p95/max to `148.15/264.79/401.51 ms` because RX capture/control tails dominated. It did keep `read_sc16` p95 low (`6.34 ms`), but total p95 was worse than `/tmp + precreate`.
  - Rejected smaller RX batch-session window: `ANALOG_RX_BATCH_SESSION_MAX_IMAGES=8` passed 50 images in `batch-1783707616-50`, fallback `0`, but worsened IQ median/p95/max to `175.06/363.43/655.65 ms`. Keep the window at `16`.
- 2026-07-11 early-morning continuation:
  - Repeated soft-completion and persistent probe path: `RemoteAnalogDecodeWorker` now rechecks soft completion until stdout arrives or the main decode timeout expires, instead of probing only once. This targets the observed case where the board output appears shortly after the first soft probe. Targeted tests covering repeated soft completion and persistent probe behavior passed.
  - Cockpit IQ defaults now explicitly include `ANALOG_RX_SESSION_CONTROL=1`, `RX_ARM_WAIT_MS=150`, `RX_STOP_WAIT_MS=8000`, `ANALOG_RX_POST_QUANTIZE=0`, `ANALOG_REMOTE_DECODE_SOFT_COMPLETE_SEC=0.05`, arm-status polling, and STOP drain defaults. The 20-image restart smoke `batch-1783720370-20` verified the arm-wait path with `rx_server_arm_wait_ms` p95 below `1 ms`; a later failed 300-image run exposed that `ANALOG_RX_POST_QUANTIZE=0` was still missing and has now been fixed.
  - Not-armed recovery now keeps the fast short-STOP path but prevents immediate `capture_already_running` retries: if short STOP leaves the final RX state busy, the runner escalates to the configured full STOP drain before the next ARQ attempt. Local RED/GREEN tests are `test_rx_control_response_busy_uses_latest_busy_token` and `test_process_image_escalates_arm_failure_stop_when_rx_remains_busy`.
  - Validation after this local fix: `python -m pytest USRP292x/test_analog_latent_link.py -q` -> `116 passed`; server IQ-default subset -> `4 passed`; `python -m pytest docker/test_demo_scripts.py -q` -> `8 passed`; `py_compile` passed; `git diff --check` only reported CRLF/LF warnings; QPSK diff stayed empty. A fresh 20/300-image hardware run is still required before promotion.
- Afternoon IQ direct optimization pass, Cockpit-button-equivalent API path:
  - `batch-1783665498-50`: minimal decode response plus `RX_ARM_WAIT_MS=50` baseline, all-pass, TVM median `240.00 ms`, IQ transport median `178.07 ms`, p95 `456.49 ms`, max `1165.38 ms`. The worst tail was still `rx_arm/rx_capture`, not TVM.
  - `batch-1783666694-50`: added opt-in RX CAPTURE preconnect with cleanup fixed back to `skip`, all-pass, TVM median `243.24 ms`, IQ transport median `188.29 ms`, p95 `461.52 ms`. It removed some CAPTURE setup cost but did not improve p95 enough, and one low-sync retry dominated max.
  - `batch-1783666884-50`: switched to `ANALOG_RX_SESSION_CONTROL=1`, all-pass, TVM median `241.11 ms`, IQ transport median `187.28 ms`, p95 `313.76 ms`, max `388.41 ms`. This proved same-session `CAPTURE`/`WAIT` is the better control-path optimization than one-shot CAPTURE preconnect.
  - `batch-1783666960-300`: session-control 300-image gate, all-pass, TVM median `242.32 ms`, IQ per-image transport median `174.30 ms`, p95 `295.98 ms`, but image `150` showed STOP cleanup ordering risk after `RX CAPTURE did not arm before TX`.
  - `batch-1783667485-300`: rejected validation after Python session-close fix but without the stable 8-second STOP drain; transport ended `299/300` with fallback `1` because image `288` remained `capture_already_running`.
  - `batch-1783668000-300`: accepted validation with session-close-before-STOP and `ANALOG_RX_STOP_DRAIN_TIMEOUT_SEC=8.0`; `300/300`, fallback `0`, TVM median `241.21 ms`, p95 `243.94 ms`, IQ transport median `169.42 ms`, p95 `304.21 ms`.
  - `batch-1783671675-50`: container/Cockpit config validation after preserving IQ/USRP/Docker env and default latent discovery; `50/50`, fallback `0`, TVM median `241.79 ms`, p95 `249.88 ms`, IQ transport median `188.76 ms`, p95 `372.30 ms`.
  - `batch-1783671744-300`: accepted container-migration validation with Docker TX and board venv RX; `300/300`, fallback `0`, TVM median `241.04 ms`, p95 `243.75 ms`, IQ transport median `171.54 ms`, p95 `363.50 ms`, remote decode median `60.89 ms`, board-reported decode median `43.58 ms`.
  - `batch-1783672570-50`: raised `RX_ARM_WAIT_MS` to `150`; `50/50`, fallback `0`, TVM median `241.58 ms`, p95 `248.65 ms`, IQ transport median `169.31 ms`, p95 `245.63 ms`, no extra retry records.
  - `batch-1783672642-300`: `RX_ARM_WAIT_MS=150` 300-image gate; `300/300`, fallback `0`, TVM median `241.93 ms`, p95 `248.18 ms`, IQ transport median `170.84 ms`, p95 `332.98 ms`. It still had `5` recovered extra attempts, so this is a profile improvement, not the final tail fix.
- Session-close tail mitigation: `ControlSession.close()` now calls `shutdown(SHUT_RDWR)` before `close()` so the RX server can observe EOF promptly after a same-session `STATUS` timeout. Smoke `batch-1783674212-50` stayed `50/50`, fallback `0`, TVM median `238.58 ms`, IQ transport median `182.34 ms`, p95 `251.66 ms`, and produced no extra retry records. Repeat gate `batch-1783674397-300` stayed `300/300`, fallback `0`, TVM median `238.87 ms`, p95 `245.54 ms`, IQ transport median `175.28 ms`, p95 `337.46 ms`, and still had `305` stage records. Keep the socket shutdown fix for cleanup correctness, but do not treat it as the final tail fix.
- RX server timing instrumentation: `OtaRxPersistentServer` now reports additive `arm_wait_sec`, `drain_sec`, `stream_cmd_sec`, `receive_sec`, `stop_cmd_sec`, and `stop_wait_sec` fields in control snapshots. `RunAnalogLatentBatch.py` records them as `rx_server_*_wall_sec` fields and exposes them in `iq_stage_benchmark` as `rx_server_*_ms`. This does not tune timing yet; it makes the next 50/300-image run distinguish drain, stream-command, receive-loop, and STOP-wait tails.
- Instrumentation smoke `batch-1783676258-50`: after syncing and rebuilding the board RX server, the Cockpit-equivalent 50-image run stayed `50/50`, fallback `0`, TVM median `242.22 ms`, p95 `243.93 ms`, IQ transport median `165.42 ms`, p95 `251.70 ms`, max `312.20 ms`, and produced exactly `50` stage records. New server medians were `rx_server_arm_wait_ms=0.66`, `rx_server_drain_ms=0.03`, `rx_server_stream_cmd_ms=0.16`, `rx_server_receive_ms=63.61`, and `rx_server_capture_ms=64.27`. This points the normal RX floor at capture duration / `ANALOG_RX_TAIL_SEC=0.05`, not server-side pre-arm drain.
- Shorter RX tail rejection: a correct-profile `ANALOG_RX_TAIL_SEC=0.04` 5-image sanity run (`batch-1783677934-5`) hit one `no sync candidate had a complete frame` retry and produced IQ median `326.22 ms`; `ANALOG_RX_TAIL_SEC=0.045` then passed 50 images (`batch-1783678227-50`) but worsened IQ median/p95 to `201.17/1207.08 ms` versus the `0.05` instrumentation baseline `165.42/251.70 ms`. Keep `ANALOG_RX_TAIL_SEC=0.05`.
- SSH ControlMaster false-tail fix: omitting `SSH_WITH_PASSWORD_DISABLE_CONTROLMASTER=1` on Windows/password SSH made the IQ runner retry ControlMaster startup per image and add about `10 s/image`. `RunAnalogLatentBatch.py` now marks batch-level ControlMaster failure as unavailable so per-image processing does not repeat the failed startup.
- No-poll diagnostic `batch-1783678924-50`: correct `0.05` profile with no mid-run status polling stayed `50/50`, fallback `0`, TVM median/p95 `239.96/245.99 ms`, IQ median/p95/max `183.51/338.34/501.46 ms`, and produced no retry records. It did not improve the tail versus `batch-1783676258-50`; the slowest sample still had server capture around `64 ms` while runner-side `rx_capture/rx_wait` and decode response wait expanded. Status polling is not the primary cause.
- Runner overhead instrumentation: `iq_stage_benchmark` now adds derived `rx_capture_control_overhead_ms` and `remote_decode_response_overhead_ms`. These diagnostic estimates compare runner-side capture/decode waits with server capture and board-reported decode time before the next RX health fix.
- Overhead-field validation `batch-1783680558-50`: the Cockpit-equivalent run stayed `50/50`, fallback `0`, TVM median/p95 `240.13/244.14 ms`, image-level IQ median/p95/max `207.72/334.49/1129.46 ms`, and produced `51` stage records for `50` images. The recovered retry was image `29`: attempt 1 failed `no sync candidate had a complete frame` with `rx_capture_control_overhead_ms=408.31`; attempt 2 succeeded but still showed `remote_decode_response_overhead_ms=123.94`. Stage p95s were `rx_capture_control_overhead_ms=140.31` and `remote_decode_response_overhead_ms=128.12`, while server capture stayed `64.21/64.35 ms` median/p95. Next fix should prioritize RX arm/capture readiness and retry cleanup, then cap decode response wait.
- Decode-failure RX cleanup: after a successful RX `WAIT`, failed decode/no-sync paths now send `STOP` and drain the RX server before ARQ retry. Unit tests cover remote decode exceptions and local decode status failures. Cockpit-equivalent validation `batch-1783681389-50` stayed `50/50`, fallback `0`, TVM median/p95 `240.73/245.35 ms`, IQ image-level median/p95/max `174.91/269.24/347.72 ms`, and produced exactly `50` stage records. Server capture stayed stable at `64.23 ms` median; `rx_capture_control_overhead_ms` p95 was `48.65 ms` and `remote_decode_response_overhead_ms` p95 was `52.45 ms`. This proves the normal path did not regress; the STOP/drain benefit still needs confirmation on the next no-sync/retry occurrence or a targeted failure-injection run.
- Post-cleanup 300-image gate `batch-1783682083-300`: transport and TVM both completed `300/300`, fallback `0`. TVM median/p95 was `243.38/255.24 ms`; IQ image-level median/p95/max was `174.42/308.45/8377.89 ms`; transport summary had `305` stage records for `300` images. The extra attempts were low-sync retry, arm-not-ready retry, and one `WAIT timeout=1.0` recovery. The WAIT timeout path did call STOP, but `rx_stop_after_wait_timeout.log` showed the STOP control command itself timed out because the runner capped the STOP client socket at `5 s` while the active profile allows `ANALOG_RX_STOP_DRAIN_TIMEOUT_SEC=8.0`. `stop_rx_capture()` now sizes the STOP client timeout to `max(base_timeout, drain_timeout + 0.5)`. Remaining max outliers include decode-worker response wait (`image 218`, runner decode `7.268 s` vs board reported `43 ms`) and one true board decode stall (`image 46`, reported `4.092 s`).
- STOP-timeout-budget validation `batch-1783682666-300`: transport and TVM both completed `300/300`, fallback `0`. TVM median/p95 was `240.17/243.93 ms`; IQ image-level median/p95/max was `172.39/310.55/19107.42 ms`; transport summary had `302` stage records. The remaining huge max came from image `196`: attempt 1 hit `WAIT timeout=1.0` and STOP still timed out, then attempt 2 succeeded but spent `6.956 s` in runner-side decode wait while board reported `58.72 ms`. Root cause update: with `ANALOG_RX_SESSION_CONTROL=1`, WAIT timeout cleanup opened a new STOP connection before closing the old RX session. The runner now closes the RX session before direct STOP in both depth-1 `process_image()` and pipeline capture attempts.
- Session-before-STOP validation `batch-1783683491-300`: transport and TVM both completed `300/300`, fallback `0`. TVM median/p95 was `242.38/245.50 ms`; IQ image-level median/p95/max was `169.48/299.73/6822.54 ms`; transport summary had `301` stage record for `300` images. The single retry was image `263` after arm/status timeout; `rx_stop_after_capture_busy.log` returned `OK` instead of `ERR_TIMEOUT`, confirming cleanup order is improved. Remaining top outliers are decode-side: true board decode stalls (`image 184` reported `6.466 s`, `image 179` reported `3.713 s`) and runner-side decode response wait (`image 156` response overhead about `1.037 s`).
- Tmpfs decoded-output recheck after RX cleanup: the first 50-image request `batch-1783683897-50` did not actually override `REMOTE_USRP_RX_DIR` because `/api/run-inference-batch` ignores `remote_usrp_rx_dir`; it repeated `/home/user/cockpit_usrp_rx` and stayed `50/50`, IQ median/p95 `182.45/317.56 ms`. The real tmpfs smoke used `/api/session/board-access` first, then ran `batch-1783684070-50`: `50/50`, fallback `0`, TVM median/p95 `241.53/246.04 ms`, IQ median/p95/max `179.92/360.48/3979.87 ms`, stage records `51`. Tmpfs capped `write_npz` at median/p95/max `1.446/2.553/2.719 ms`, but total transport worsened due an arm-not-ready retry and decode response p95 `125.67 ms`. Keep tmpfs diagnostic-only, not default.
- Soft-completion observability: remote-dir soft completion now returns `soft_completed=true`, and per-image records include `remote_decode_soft_completed`. At this historical point the default profile still left `ANALOG_REMOTE_DECODE_SOFT_COMPLETE_SEC=0`; the 7/11 candidate later moved to persistent-probe repeated soft completion at `0.05`.
- Decode-side timing observability: board `decode_summary.json` now separates matched-filter timing, runner records sanitized `remote_decode_timing_ms`, and `iq_stage_benchmark` emits `remote_decode_matched_filter_ms`, `remote_decode_initial_sync_ms`, `remote_decode_cfo_estimate_ms`, `remote_decode_payload_recovery_ms`, `remote_decode_write_npz_ms`, and adjacent decode-stage metrics. This does not change the default profile; it makes the next 50/300-image gate identify whether decode tails are PHY search, CFO, payload recovery, file publish, or runner response wait.
- Decode timing 50-image smoke `batch-1783686020-50`: transport and TVM both completed `50/50`, fallback `0`. TVM median/p95 was `239.71/250.01 ms`; IQ total median/p95/max was `171.80/324.63/1417.55 ms`; stage records were `51` because image `0` had one low-sync retry before success. New decode-stage fields were present. Board decode internals were not the main p95 tail: matched filter p95 `8.97 ms`, initial sync `6.50 ms`, CFO `4.01 ms`, payload recovery `37.73 ms`, `.npz` write `3.28 ms`, board-reported decode p95 `88.40 ms`. The larger tails were `rx_capture_control_overhead_ms` p95 `124.74 ms`, `remote_decode_response_overhead_ms` p95 `121.63 ms`, and the failed low-sync attempt whose `decode_wall_sec` was previously unmeasured.
- Failed remote-decode attempts now preserve `decode_wall_sec` when the worker raises before the normal success record is built. This closes the attribution gap seen in `batch-1783686020-50` where a low-sync failed attempt consumed wall time but recorded `decode_wall_sec=0`.
- Decode timing 300-image gate `batch-1783686930-300`: transport and TVM both completed `300/300`, fallback `0`, all-pass `true`, stage records `300`. TVM median/p95/max was `240.29/242.87/257.55 ms`; IQ total median/p95/max was `166.04/288.97/7683.31 ms`. The median target is now clearly below TVM, but p95/max still need work. Decode internals were normally bounded: matched filter p95 `3.67 ms`, initial sync `4.01 ms`, payload recovery `19.31 ms`, `.npz` write p95 `2.94 ms`, board-reported decode p95 `56.37 ms`. The max was image `177`, where board `write_npz` took `7440.19 ms` and total IQ hit `7683.31 ms`. Other top tails were RX control/capture stalls: images `103`, `110`, and `235` had `rx_arm_ms` about `1025-1122 ms` while server capture stayed about `64 ms`; image `184` had runner decode wait `1231.19 ms` with board-reported decode `56.37 ms`.
- Resulting recommendation: promote `ANALOG_RX_SESSION_CONTROL=1` and keep `ANALOG_PRECONNECT_RX_CAPTURE_CONTROL=0` for the Cockpit IQ profile. CAPTURE preconnect remains an opt-in diagnostic; it is not the default because same-session control gave lower p95 with cleaner RX wait/capture stages.
- Remaining root cause: server-side wait-on-STOP now prevents stale capture state from breaking the batch, but rare RX arm/capture stalls, runner decode-response waits, and a rare `/home` `.npz` write stall still create p95/max values above TVM. Next optimization should focus on RX readiness/control-session latency and decoded-output publish placement/format, not QPSK.

- Implemented stage-level IQ metrics, board decode remote-dir streaming visibility, `ANALOG_PIPELINE_DEPTH=2`, atomic `.npz` publish, dynamic big.LITTLE input waiting, and opt-in IQ streaming TVM gates.
- `OPENAMP_IQ_STREAMING_TVM` / `USRP_IQ_STREAMING_TVM` default to off. The overlap path remains available for experiments, but it is not the recommended cockpit profile because it contends with board decode and worsens both TVM p95 and total wall time.
- 20-image smoke after gating: `batch-1783625245-20`, `20/20`, fallback `0`, TVM median `242.22 ms`, p95 `255.58 ms`; transport completed before TVM wrapper startup.
- 300-image validation after gating: `batch-1783625337-300`, `300/300`, fallback `0`, TVM median `242.16 ms`, transport median `189.10 ms`, total wall `161.17 s`.
- Evidence of no overlap in the previous sequential profile: poll sequence stayed `inference=0/300 pending` while transport advanced `69 -> 156 -> 224 -> 296/300`; inference entered `running` only after transport became `300/300 completed`. The current fast-first profile keeps the same streaming-TV-off default.
- Fast-first sync implementation and validation: `batch-1783626884-300`, `300/300`, fallback `0`, TVM median `242.41 ms`, transport median `182.28 ms`, transport p95 `372.96 ms`, total wall `157.12 s`.
- Fast-first sync pass evidence: all 300 image summaries used `sync_profile=fast-first` and `sync_pass=1`; `fast_sync_ms` median `21.07 ms`, p95 `61.51 ms`, selected sync metric median `0.9390`, minimum `0.0508`.
- Remaining tail evidence: image `0194` had `fast_sync_ms=3033.38` and `decode_total_ms=4987.12`; stage summary max values also show `rx_capture_ms=4946.78` and `remote_decode_ms=5002.67`. The median and p95 are now acceptable, but the next pass should investigate rare RX/decode stalls.
- Added queue attribution for the double-buffered path: `remote_decode_queue_ms` now separates "captured and waiting for the single decode worker" from RX capture and decode execution. This showed that image `0195` in `batch-1783626884-300` was mostly waiting behind the slow image `0194` decode, not itself slow to decode.
- Burst-miss retry experiment: batch `batch-1783627996-50` without the new guard had transport median `283.31 ms`, p95 `326.58 ms`, decode max `674.28 ms`, and one `sync_pass=2` burst-miss success with `decode_total_ms=655.68`. Batch `batch-1783628596-50` with `ANALOG_RETRY_ON_BURST_MISS=1` had transport median `199.90 ms`, decode max `197.08 ms`, zero burst-miss successes, and one IQ retry, but transport p95 worsened to `783.40 ms` due to RX capture outliers. Keep this guard opt-in until a 300-image run proves the RX tail is stable.
- RX tail diagnosis from existing logs found two stall classes. In `batch-1783626884-300`, image `0194` had a real RX wait stall (`rx_wait.log wall_sec=4.91059`, runner `rx_capture_ms=4946.78`), while image `0066` showed runner-side capture wall `1100.61 ms` with normal RX server wait (`rx_wait.log wall_sec=0.0742965`). In `batch-1783628596-50`, image `0010` similarly showed runner-side `rx_capture_ms=1204.09` while server-side wait stayed near `74 ms`.
- Added runner/cockpit benchmark split for `rx_arm_ms` and `rx_wait_ms` while preserving the existing `rx_capture_ms` end-to-end capture window. The next live run should show whether the remaining tail comes from repeated control socket setup/scheduling, actual RX wait, or both.
- Control-split 50-image baseline: `batch-1783629764-50`, `50/50`, fallback `0`, transport median `206.51 ms`, p95 `308.24 ms`, max `350.17 ms`; `tx_control_ms` median `31.42`, `rx_arm_ms` median `35.27`, `rx_wait_ms` median `30.75`, and `rx_capture_ms` median `110.41`.
- Opt-in Python-side control preconnect experiment: `batch-1783631315-50` with `ANALOG_PRECONNECT_CONTROL=1`, `50/50`, fallback `0`, transport median `207.08 ms`, p95 `290.16 ms`, max `322.92 ms`; `tx_control_ms` median improved to `26.33`, but `rx_wait_ms` median worsened to `36.34`, so the net median did not improve. Keep this opt-in for experiments only.
- Opt-in RX same-session control experiment: `batch-1783632169-50` with `ANALOG_RX_SESSION_CONTROL=1`, `50/50`, fallback `0`, but transport worsened to median `287.71 ms`, p95 `462.23 ms`, max `1138.94 ms`. Stage medians were `tx_control_ms=38.10`, `rx_arm_ms=33.78`, `rx_capture_ms=118.05`, `rx_wait_ms=32.71`, `remote_decode_queue_ms=0.06`, and `remote_decode_ms=74.08`. One image retried after low sync metric, and the largest tail came from decode queue delay plus RX capture/wait spikes. Keep this opt-in and unset by default.
- Local x86_64 and board aarch64 `OtaRxPersistentServer` / `OtaTxPersistentServer` builds now support multiple commands on one client connection. This is backward-compatible with the existing one-command clients, but the live RX same-session measurement did not justify enabling it in the recommended profile.
- Opt-in low-sync early retry experiment: `ANALOG_RETRY_ON_LOW_SYNC=1` skips slow fallback decode when the fast-sync initial metric is below `ANALOG_LOW_SYNC_RETRY_THRESHOLD` and another ARQ capture remains. Threshold `0.08` on `batch-1783633562-50` stayed all-pass but worsened median to `259.08 ms`; threshold `0.05` on `batch-1783633743-50` stayed all-pass with median `210.61 ms`, p95 `367.05 ms`, max `419.52 ms`, and no actual early-retry triggers. The 300-image validation `batch-1783633910-300` failed `299/300`, fallback `1`, with transport median `196.87 ms`, p95 `497.47 ms`, and max `11379.71 ms`. Keep this opt-in and unset by default.
- New evidence from `batch-1783633910-300`: low sync is not the only tail source. Image `0267` had high sync metric `0.9655` but `remote_decode_ms=9066.29`; image `0268` had RX wait/capture stalls across retries; image `0154` exhausted all three attempts after bad captures. The next optimization should target RX capture health and decode worker stall detection, not more aggressive low-sync retry.
- Added `remote_decode_reported_ms` to `iq_stage_benchmark`. This separates runner-side wait for the decode worker from the board decoder's own `decode_total_ms`; it is needed because image `0267` reported only `51.41 ms` inside `decode_summary.json` while the runner waited `9066.29 ms`.
- Added opt-in remote decode worker health controls: `ANALOG_REMOTE_DECODE_REQUEST_TIMEOUT_SEC` limits a single persistent-worker request, and `ANALOG_REMOTE_DECODE_RESTART_ON_TIMEOUT=1` restarts the worker after that timeout so a later ARQ attempt can continue. Keep both unset in the recommended profile until a live run proves the timeout does not reduce all-pass reliability.
- Decode-worker timeout validation: `batch-1783635469-50` with `ANALOG_REMOTE_DECODE_REQUEST_TIMEOUT_SEC=1.0` and restart enabled stayed `50/50`, but no timeout fired; transport median `206.40 ms`, p95 `456.82 ms`, max `961.57 ms`.
- Decode-worker timeout 300-image validation: `batch-1783635568-300` stayed `300/300` and fired 3 worker timeouts, capping the timeout attempts near `1.02 s` of `remote_decode_ms`. It did not become the default because transport p95 worsened to `865.77 ms` and max reached `13835.21 ms`; synchronous worker restart and RX capture stalls still propagated into `remote_decode_queue_ms`.
- Added `remote_decode_restart_ms` attribution for timeout records. This explains the gap between `decode_wall_sec` and `total_wall_sec` when a timed-out persistent worker is killed and restarted before the ARQ retry can proceed.
- Early pipeline-depth diagnostic: `ANALOG_PIPELINE_DEPTH=1`, `batch-1783636039-300`, stayed `300/300` and lowered transport p95 to `318.43 ms`, but batch wall worsened to `104.43 s` and max still reached `10070.85 ms`. At that point depth 1 was only a diagnostic; later RX-arm evidence changed the recommendation.
- RX tail experiment: `ANALOG_RX_TAIL_SEC=0.02`, `batch-1783636392-50`, failed badly (`28/50`) with many `no sync candidate had a complete frame` errors. `ANALOG_RX_TAIL_SEC=0.04`, `batch-1783636541-50`, stayed `50/50` but slowed to median `241.27 ms`, p95 `1169.05 ms`, with 10 retried images. Keep the recommended tail at `0.05`.
- Post-quantize-off experiment: `ANALOG_RX_POST_QUANTIZE=0` keeps the `latent` array consumed by TVM but stops writing unused `quant/scale/zero_point` arrays. The 50-image run `batch-1783638107-50` stayed all-pass with TVM median `241.26 ms`, PSNR `37.0445`, SSIM `0.97494`, and `write_npz` p95 `3.50 ms`. The 300-image run `batch-1783638234-300` stayed all-pass with transport median `186.83 ms`, p95 `351.67 ms`, TVM median `241.28 ms`, p95 `244.10 ms`, PSNR `37.0445`, and SSIM `0.97494`. Make this the recommended cockpit profile.
- Remote-dir `.npy` experiment: added `ANALOG_REMOTE_DECODED_FORMAT=npy` to `RunAnalogLatentBatch.py`, `AnalogLatentLink.py`, Cockpit runtime, and Docker wrapper pass-through. The `.npy` format is compatible with the existing big.LITTLE loader because it already accepts `*.npy`.
- `.npy` evidence on `/home`: 50-image run `batch-1783639944-50` stayed all-pass with transport median `192.45 ms`, p95 `372.33 ms`, write p95 `1.48 ms`; 300-image run `batch-1783640049-300` stayed all-pass with transport median `189.19 ms`, p95 `383.88 ms`, TVM p95 `248.64 ms`, write p95 `1.64 ms`, but one board filesystem stall still made write max `3337.46 ms`.
- tmpfs decoded-output evidence: board access `remote_usrp_rx_dir=/dev/shm/cockpit_usrp_rx` run `batch-1783640318-50` stayed all-pass with transport median `191.32 ms`, p95 `295.27 ms`, write p95 `1.27 ms`, max `1.68 ms`. The 300-image run `batch-1783640401-300` stayed all-pass and capped write max at `3.43 ms`, but transport p95 worsened to `1013.67 ms` due RX/no-sync stalls. Use tmpfs as an I/O diagnostic, not as the recommended profile yet.
- Added opt-in board stall snapshots for the next IQ live run. Set `ANALOG_REMOTE_STALL_SNAPSHOT=1` to collect `top`, `vmstat`, memory, filesystem, and hot-process status when an image record exceeds `ANALOG_REMOTE_STALL_SNAPSHOT_THRESHOLD_SEC` (default `1.0`) or records an error. The runner limits this to `ANALOG_REMOTE_STALL_SNAPSHOT_LIMIT` snapshots per process (default `3`) and writes the local log path plus trigger fields into the image round record. Keep this off in recommended performance runs unless diagnosing p95/max outliers.
- First stall-snapshot live run: `batch-1783641924-50`, `/home/user/cockpit_usrp_rx`, `.npz`, `50/50`, fallback `0`, TVM median `239.75 ms`, p95 `248.56 ms`. It captured board status for images `31`, `32`, and `33`. Image `31` was a real RX wait/capture stall (`rx_wait_ms=1146.59`, `rx_capture_ms=1211.22`) while the board was mostly idle after the event; images `32` and `33` were queue propagation behind that stall. The first implementation used synchronous SSH snapshots, adding about `2.4-2.8 s` snapshot wall time and inflating `remote_decode_queue_ms`; the snapshot launcher is now asynchronous so diagnostics do not block the decode worker.
- Async snapshot validation: `batch-1783642466-50`, `/home/user/cockpit_usrp_rx`, `.npz`, `50/50`, fallback `0`, TVM median `243.89 ms`, p95 `246.91 ms`, transport median `190.41 ms`, p95 `360.71 ms`, max `399.43 ms`. No snapshot fired because no record crossed the `1.0 s` threshold. `remote_decode_queue_ms` max dropped from the synchronous snapshot run's `2732.29 ms` to `92.41 ms`, confirming the diagnostic no longer blocks the pipeline.
- Async snapshot 300-image diagnostic: `batch-1783642640-300`, `/home/user/cockpit_usrp_rx`, `.npz`, `300/300`, fallback `0`, TVM median `240.79 ms`, p95 `243.97 ms`, PSNR `37.0445`, SSIM `0.97494`. Transport median stayed `190.29 ms`, but p95 rose to `946.07 ms` and max `6524.46 ms` because 8 retry rounds and rare long stalls were present. The largest classes were real RX wait/capture stalls (`image 201 rx_wait_ms=6383.95`, `image 0 rx_wait_ms=3035.10`), decode-worker request stalls where board-reported decode stayed about `42 ms` but runner waited seconds (`image 297 decode_ms=5453.34`, reported `42.20`; `image 290 decode_ms=3602.09`, reported `41.69`), and one true board decode compute stall (`image 281 reported decode=4657.48 ms`). Snapshot launch was asynchronous (`~23-29 ms` launch wall), but the limit of 3 only captured the earliest anomalies; use a higher limit for the next diagnostic if late-batch outliers matter.
- Added opt-in short RX WAIT budgeting: `ANALOG_RX_WAIT_TIMEOUT_SEC` / `--rx-wait-timeout-sec`. Default `0` keeps the existing wide capture timeout. A positive value only shortens the RX `WAIT timeout=...` command so an unusually late capture can fail the current ARQ attempt quickly and retry, instead of blocking for 3-6 seconds.
- Short RX WAIT 50-image validation: `batch-1783643493-50` with `ANALOG_RX_WAIT_TIMEOUT_SEC=0.5` stayed `50/50`, fallback `0`, and every round record used `rx_wait_timeout_sec=0.5`. Transport median was `195.55 ms`, p95 `322.94 ms`, max `393.82 ms`; `rx_wait_ms` max was only `55.34 ms`. TVM median was `252.77 ms` and p95 `325.67 ms`, so this remains an opt-in transport-tail experiment until a 300-image run proves TVM stability and all-pass reliability.
- Short RX WAIT 300-image validation failed: `batch-1783643637-300` with `ANALOG_RX_WAIT_TIMEOUT_SEC=0.5` decoded only `284/300` and fell back for 16 images. The first failures were `WAIT timeout=0.500000`; because the RX server continued its capture after the client timeout, following images hit `ERR error=capture_already_running`. Do not use short WAIT as a profile until the RX server supports an explicit cancel/drain command or the runner can safely wait for capture completion before starting the next slot.
- Added short-WAIT cleanup guard: on WAIT timeout the IQ runner now sends RX `STOP` before surfacing the failed attempt, and pipeline capture-stage exceptions can retry the same image/slot while ARQ attempts remain. Validation tests cover both single-image and pipeline paths.
- Post-cleanup short WAIT 50-image cockpit API validation: `batch-1783644743-50`, `ANALOG_RX_WAIT_TIMEOUT_SEC=0.5`, `50/50`, fail `0`, fallback `0`, pipeline depth `2`, remote-dir `.npz`, `ANALOG_RX_POST_QUANTIZE=0`, fast-first sync. Transport median `175.55 ms`, p95 `295.71 ms`, max `329.66 ms`; `rx_wait_ms` median `28.34`, p95 `41.28`, max `44.05`; board decode median `62.38 ms`; TVM processed `50/50` with median `242.24 ms`, p95 `243.94 ms`. No WAIT timeout records and no STOP logs appeared in this passing run.
- Post-cleanup short WAIT 300-image cockpit API validation still failed: `batch-1783645201-300`, `281/300`, fail `19`, all-pass `false`. Transport median improved to `178.33 ms`, but p95 was `843.55 ms`; the summary had `352` records, `4` WAIT timeout/busy records, `51` capture-busy records, and `3` STOP logs. Root cause: one failure mode returned `WAIT timeout=0.500000` with `ERR busy=1 ... job_id=3099` rather than `ERR_TIMEOUT`, so the first cleanup guard did not STOP the RX server. Images `218-234` then exhausted retries against `ERR error=capture_already_running`; images `285` and `289` failed sync/no-frame independently.
- Added the second cleanup guard after `batch-1783645201-300`: treat `WAIT timeout + ERR busy` as STOP-worthy, and send STOP after `CAPTURE ... capture_already_running` before the pipeline ARQ retry loop continues. This keeps `ANALOG_RX_WAIT_TIMEOUT_SEC=0.5` experimental; it must pass a fresh 300-image cockpit API run before becoming a candidate default.
- Persistent RX server recovery fix: `OtaRxPersistentServer.cpp` startup-drain handling now has a deadline, checks `stop_requested_`, and uses non-blocking `recv(..., 0.0)` before stream start. This prevents STOP from hanging behind residual drain work when a WAIT timeout or busy cleanup happens before the capture is fully started.
- RX arm barrier fix: `RunAnalogLatentBatch.py` now parses RX control responses and treats `CAPTURE` responses without `started=1` or `done=1` as not armed. The runner polls `STATUS` before TX, and if arm status times out it records `rx_arm_status.log`, sends `STOP`, and retries through the existing ARQ path.
- Depth-2 recovery evidence after the C++ and Python fixes: `batch-1783648476-300` reached `298/300`; `batch-1783649095-300` reached `299/300`; `batch-1783649536-300` reached `298/300`; `batch-1783650496-300` reached `297/300`. These runs proved the new cleanup paths work, but they also proved that overlapping board decode/write with the next RX arm still causes `started=0`, arm-timeout, and capture-busy chains.
- Depth-1 validation before the TX-delay change: `batch-1783650744-300`, `ANALOG_PIPELINE_DEPTH=1`, `ANALOG_RX_WAIT_TIMEOUT_SEC=1.0`, `ANALOG_RX_STOP_DRAIN_TIMEOUT_SEC=8.0`, `ANALOG_RX_POST_QUANTIZE=0`, fast-first sync, robust sync off, remote-dir `.npz`, `300/300`, fail `0`, all-pass `true`. Transport wall was `82.503 s`, `249.93 ms/image`, median `182.84 ms`, p95 `315.29 ms`; TVM big.LITTLE processed `300/300`, median `241.592 ms`, wall `76.183 s`, `253.942 ms/image`.
- New depth-2 safety optimization: add `ANALOG_PIPELINE_RF_DECODE_OVERLAP`. Default `0` serializes board decode/result publish against the USRP RX/TX critical section when `ANALOG_PIPELINE_DEPTH>1`; explicit `1` restores the old overlap behavior for A/B testing. The goal is to keep depth-2 slot buffering and local make overlap without letting board decode/write run during RX arm/WAIT.
- Cockpit runtime pass-through: `ANALOG_PIPELINE_RF_DECODE_OVERLAP=1` appends `--pipeline-rf-decode-overlap`, while `0` appends `--no-pipeline-rf-decode-overlap`. This lets Cockpit-button-equivalent runs compare guarded depth 2 against the old overlap without changing QPSK.
- Guarded depth-2 first 300-image run: `batch-1783652983-300`, `ANALOG_PIPELINE_DEPTH=2`, `ANALOG_PIPELINE_RF_DECODE_OVERLAP=0`, `ANALOG_RX_WAIT_TIMEOUT_SEC=1.0`, reached only `299/300`. Root cause was not RF loss: image `0100` attempt 2 reported local `WAIT timeout=1.0`, but `STOP` immediately observed `done=1 ok=1 wall_sec=0.076`; the client socket timeout was equal to the RX server WAIT budget and could expire before the OK response arrived.
- Added `ANALOG_RX_WAIT_CONTROL_TIMEOUT_MARGIN_SEC` with default `1.0`. The RX server still receives the short `WAIT timeout=...` budget, but the local control socket waits `budget + margin` before declaring command failure. This prevents a completed capture from being converted into a retry solely by control-plane scheduling jitter.
- Guarded depth-2 second 300-image run after the client-timeout margin: `batch-1783653522-300`, transport `300/300`, fail `0`, all-pass `true`, but transport median `244.21 ms`, p95 `847.10 ms`, max `6330.15 ms`. Error records were recovered (`3` no-sync, `2` low-sync, `1` not-armed), and the remaining long tail came from decode/request stalls plus queue propagation rather than missing outputs.
- TVM for `batch-1783653522-300`: the Cockpit-triggered automatic TVM wrapper hit a transient SSH `kex_exchange_identification: Connection closed by remote host` after the USRP stage. A TVM-only rerun with Docker SSH runner on the same remote-dir inputs succeeded: report `openamp3_usrp_1783653522_current_retry_20260710_112715.json`, `300/300`, handwritten/big.LITTLE median `243.151 ms`, mean `243.642 ms`, max `268.31 ms`, wall `77.138 s`, `257.126 ms/image`, artifact SHA matched.
- Guarded depth-2 timeout/restart experiment: `batch-1783654447-300`, `ANALOG_REMOTE_DECODE_REQUEST_TIMEOUT_SEC=1.0`, `ANALOG_REMOTE_DECODE_RESTART_ON_TIMEOUT=1`, transport `300/300`, TVM automatic wrapper `300/300`, TVM median `241.416 ms`, wall `76.186 s`. This did not improve IQ transport: median `249.61 ms`, p95 `675.01 ms`, max `20115.29 ms`. Four worker timeouts were recovered, but worker restart added multi-second stalls (`16.57 s`, `5.89 s`, `4.15 s`, `2.65 s`), so timeout/restart remains diagnostic only.
- Current conclusion: guarded depth 2 improves reliability after the client-timeout fix, but it is not faster than depth 1 and is not close to the goal of IQ transport far below TVM. Keep depth 1 as the stable Cockpit default; keep guarded depth 2 as a diagnostic until decode/request tail and sync retry behavior are reduced.
- Fresh depth-1 baseline with the client-timeout margin and no decode timeout/restart: `batch-1783655547-300`, `300/300`, fail `0`, transport median `183.53 ms`, p95 `333.82 ms`, max `7987.46 ms`; stage medians were `tx_control_ms=36.79`, `rx_arm_ms=19.03`, `rx_wait_ms=32.20`, `rx_capture_ms=96.03`, `remote_decode_ms=63.21`. TVM processed `300/300`, median `241.472 ms`, wall `76.758 s`, `255.859 ms/image`, artifact SHA matched.
- Zero fixed TX-delay hypothesis: with the RX arm barrier in place, the legacy `PERSISTENT_RX_TX_DELAY=0.010` safety sleep should be redundant because TX is already held until the RX server reports armed. This is an environment-only change and does not touch QPSK.
- Zero TX-delay 50-image smoke: `batch-1783655890-50`, `PERSISTENT_RX_TX_DELAY=0.0`, `50/50`, fail `0`, transport median `176.81 ms`, p95 `221.04 ms`, max `328.83 ms`; `rx_capture_ms` median dropped to `84.18 ms`. One recovered no-sync record appeared, so the profile required a 300-image gate before promotion.
- Zero TX-delay 300-image validation: `batch-1783655994-300`, `PERSISTENT_RX_TX_DELAY=0.0`, `300/300`, fail `0`, all-pass `true`. Transport wall was `78.466 s`, mean `233.58 ms/image`, median `171.31 ms`, p95 `300.72 ms`, max `4813.22 ms`; stage medians were `tx_control_ms=41.19`, `rx_arm_ms=19.84`, `rx_wait_ms=28.63`, `rx_capture_ms=88.18`, and `remote_decode_ms=60.52`.
- TVM for `batch-1783655994-300`: automatic Cockpit-triggered big.LITTLE wrapper processed `300/300`, median `242.066 ms`, p95 `245.107 ms`, max `259.238 ms`, wall `76.437 s`, `254.789 ms/image`, and `artifact_sha256_match=true`. This keeps the handwritten/TVM chain unchanged while IQ transport median is now about `70.8 ms` below TVM median.
- Historical recommendation before the 7/11 `.npy`/mmap/bounded-session pass: keep depth 1, `ANALOG_PIPELINE_RF_DECODE_OVERLAP=0`, `ANALOG_RX_WAIT_TIMEOUT_SEC=1.0`, `ANALOG_RX_WAIT_CONTROL_TIMEOUT_MARGIN_SEC=1.0`, `ANALOG_RX_STOP_DRAIN_TIMEOUT_SEC=8.0`, `ANALOG_RX_POST_QUANTIZE=0`, `PERSISTENT_RX_TX_DELAY=0.0`, and `ANALOG_PRECONNECT_CONTROL=1` for Cockpit IQ direct speed tests. The current 7/11 candidate supersedes this with `.npy`, mmap, bounded RX session, response-only summary, persistent-probe soft completion at `0.05`, and explicit `RX_ARM_WAIT_MS=150`.
- Preconnect-on zero-delay 50-image smoke: `batch-1783656535-50`, `ANALOG_PRECONNECT_CONTROL=1`, `50/50`, fail `0`, no retry/error records. Transport median improved to `168.85 ms`, p95 `256.36 ms`; `tx_control_ms` median dropped to `27.38 ms`, while `rx_wait_ms` rose to `34.99 ms`.
- Preconnect-on zero-delay 300-image validation: `batch-1783656629-300`, `300/300`, fail `0`, all-pass `true`, no retry/error records. Transport median/p95 improved to `167.36 ms` / `296.90 ms`, and `tx_control_ms` median dropped to `25.01 ms`, but mean/wall worsened to `243.58 ms/image` / `80.150 s`. The top three outliers were not board compute: runner `decode_wall_sec` was `3.007`, `4.919`, and `5.513 s`, while `remote_decode_reported_wall_sec` was only `0.040`, `0.056`, and `0.044 s`.
- Preconnect repeat 300-image validation: `batch-1783658102-300`, `300/300`, fail `0`, all-pass `true`, recovered low-sync records `3`. Transport improved to median `163.83 ms`, p95 `284.45 ms`, wall `77.362 s`; `tx_control_ms` median stayed low at `25.10 ms`. TVM processed `300/300`, median `239.510 ms`, wall `75.201 s`, artifact SHA matched. The remaining max tail came from real board decode (`image 187`, board-reported `7.419 s`), low-sync retry (`image 164`), and RX wait spikes, not from normal-path control overhead.
- Decode-worker response-tail guard: `RemoteAnalogDecodeWorker` now matches responses by `request_id` or `summary_json` and stores stale responses instead of treating a late previous response as the current image. This was added with a failing unit test first and is a prerequisite for any future remote-dir soft-completion path that would proceed when the board `.npz` and `decode_summary.json` already exist.
- Opt-in remote-dir soft completion: added `ANALOG_REMOTE_DECODE_SOFT_COMPLETE_SEC`. If a worker stdout response is late, the runner can check whether the board output file and remote `decode_summary.json` already exist and return a worker-shaped response without restarting the worker. This was kept disabled at that point; the current 7/11 candidate uses a narrower `0.05` persistent-probe repeated check with response-only-summary.
- Soft-completion smoke result: `batch-1783657728-50`, `ANALOG_PRECONNECT_CONTROL=1`, `ANALOG_REMOTE_DECODE_SOFT_COMPLETE_SEC=0.25`, `50/50`, fail `0`, but transport worsened to median `179.54 ms`, p95 `347.63 ms`, mean `273.25 ms/image`. It triggered only one soft-completion check. The biggest outlier was RX arm (`image 3`, `rx_arm_ms=3023.63`), and the second was real board decode (`image 8`, `remote_decode_reported_wall_sec=1.362 s`), so this run did not reproduce the stdout-late case and should not proceed to a 300-image promotion run.
- `.npy` remote-dir smoke on the promoted preconnect profile: `batch-1783658585-50`, `ANALOG_REMOTE_DECODED_FORMAT=npy`, `50/50`, fail `0`, no retry/error records, but transport median/p95 worsened to `169.95 ms` / `345.24 ms` versus the `.npz + preconnect` 300-image profile. Remote publish was not the bottleneck (`remote_dir_publish_wall_sec` around `0.7-2.3 ms` in the top records), so `.npy` should remain a diagnostic and should not proceed to 300-image validation.
- tmpfs `.npz + preconnect` smoke: `batch-1783658922-50`, `remote_usrp_rx_dir=/dev/shm/cockpit_usrp_rx`, `50/50`, fail `0`, no retry/error records. Transport median was `171.87 ms`, p95 `263.72 ms`, max `308.78 ms`, wall `14.521 s`; top decoded-output writes were only `1.4-3.1 ms`. This confirmed the `/home` tail can be an output-write problem.
- tmpfs `.npz + preconnect` 300-image gate: `batch-1783659032-300`, `300/300`, recovered `1` low-sync and `1` no-sync record. Transport median was `170.03 ms`, p95 `285.40 ms`, max `8705.21 ms`, wall `78.550 s`; TVM processed `300/300`, median `239.970 ms`, wall `75.311 s`, `251.037 ms/image`, and artifact SHA matched. The board write path stayed fast (`write_npz` about `1.4-1.9 ms` in the worst records), but the largest tails moved to worker stdout/SSH wait: the runner waited `8.151 s`, `4.431 s`, and `3.692 s` while board-reported decode was only `39-58 ms`.
- tmpfs plus soft-completion 50-image smoke: `batch-1783659322-50`, `ANALOG_REMOTE_DECODE_SOFT_COMPLETE_SEC=0.25`, `50/50`, fail `0`, transport median `172.14 ms`, p95 `242.55 ms`, max `367.48 ms`, wall `16.336 s`; no soft-completion checks fired, so the run did not prove the guard.
- tmpfs plus soft-completion 300-image gate: `batch-1783659430-300`, `300/300`, recovered `1` low-sync record. Transport median was `167.38 ms`, p95 `355.63 ms`, max `7401.60 ms`, wall `82.673 s`; TVM processed `300/300`, median `240.951 ms`, wall `75.650 s`, `252.165 ms/image`, and artifact SHA matched. Soft completion fired (`4` checks, `2` non-empty), but the worst records still had runner decode waits of `7.046 s`, `5.314 s`, and `1.487 s` while board-reported decode stayed about `40-51 ms`. Do not promote tmpfs or soft completion as default until the worker/stdout wait path is fixed without adding extra SSH checks to the hot path.
- Minimal worker response implementation: added `ANALOG_REMOTE_DECODE_RESPONSE_MODE=minimal` as an opt-in persistent-worker protocol mode. It records the mode in `batch_spool_summary.json`, sends `response_mode=minimal` in each decode request, and makes board `decode-server` return only runner-required fields in stdout while retaining the full remote `decode_summary.json`. Docker and Cockpit backend startup env pass-through now preserve the knob.
- Minimal response validation on `/home`: `batch-1783661396-50` stayed all-pass and confirmed minimal stdout responses (`remote_decode.log` median `731 bytes`). `batch-1783661701-300` stayed `300/300`, transport median `168.01 ms`, p95 `284.14 ms`, wall `72.983 s`, TVM median `242.443 ms`, and artifact SHA matched. This is a useful opt-in candidate because mean/wall improved and remote-decode p95 dropped to `84.75 ms`, but median is slightly worse than `batch-1783658102-300` and a true board write/decode stall remains, so it needs another repeat before replacing the current default.
- Minimal response validation on tmpfs: `batch-1783662016-300` stayed `300/300`, transport median `167.60 ms`, p95 `313.20 ms`, wall `74.485 s`, TVM median `241.566 ms`, and artifact SHA matched. It proved tmpfs plus minimal response removes board write and large stdout tails from the top records, but RX arm/capture stalls now dominate, so tmpfs remains diagnostic.
- Keep QPSK frozen. `USRP292x/RunQpskFileBatchSpoolArq.py` diff remained empty through the IQ changes.
- Next work: optimize IQ direct without touching QPSK by reducing weak-sync decode tails and remaining RX capture/wait tails. The current 7/11 candidate has promoted `.npy`, RX same-session control through bounded sessions, persistent-probe soft completion at `0.05`, per-probe response IDs, and precreate retries. Worker timeout/restart, tmpfs, short STOP, short RX WAIT, publish-event, and TVM/transport overlap remain diagnostics unless new 300-image all-pass measurements prove they improve p95.

## Task Status Snapshot

- Done: Task 1 runner-selection guard, Task 2 stage metrics, Task 3 warm board decode worker, Task 5 double buffering, Task 6 fast-first sync, and Task 7 cockpit-button-equivalent 20/300 validation.
- Done but not default: Task 4 remote decode to TVM streaming. It is guarded by explicit env opt-in because live measurements showed contention.
- Done and recommended for Cockpit IQ speed tests: Python control preconnect with zero TX delay. Two 300-image runs stayed all-pass and pushed median transport to `167.36 ms` and then `163.83 ms`; RX same-session control remains worse and unset.
- Done but not default: low-sync early retry. It is guarded by `ANALOG_RETRY_ON_LOW_SYNC` because the 300-image validation lost all-pass reliability.
- Done but not default: decode-worker timeout/restart and shorter RX tail. Timeout/restart preserves all-pass but worsens p95 in a 300-image run; shorter tail loses sync margin.
- Done and recommended: `ANALOG_RX_POST_QUANTIZE=0`. It reduces remote-dir `.npz` write payload without changing TVM's `latent` input contract or quality metrics.
- Done and recommended for the current stable Cockpit profile: `ANALOG_PIPELINE_DEPTH=1` with `ANALOG_RX_WAIT_TIMEOUT_SEC=1.0`, `ANALOG_RX_WAIT_CONTROL_TIMEOUT_MARGIN_SEC=1.0`, `ANALOG_RX_STOP_DRAIN_TIMEOUT_SEC=8.0`, `PERSISTENT_RX_TX_DELAY=0.0`, and `ANALOG_PRECONNECT_CONTROL=1`. This keeps the clean 300-image IQ direct run while removing the fixed 10ms TX delay and cutting normal TX control overhead.
- Done but not default: bounded RX batch sessions plus response-only-summary. The combined profile is the current best A/B baseline (`batch-1783697942-300`, IQ median/p95 `153.37/269.84 ms`), but it is still an opt-in candidate because the 300-image p95 is above TVM p95 and max latency remains multi-second.
- Done but not default: `ANALOG_REMOTE_DECODE_WORKER_PREFIX`. `taskset -c 2` produced an excellent 50-image p95 (`177.68 ms`) but the 300-image gate landed at IQ p95 `275.68 ms`, max `8884.26 ms`, still above TVM p95 and slightly worse than the prior combined candidate.
- Rejected for default: output-only remote-dir soft completion at `0.14 s`. It added extra checks without reducing the measured long tail (`batch-1783698593-50`, p95 `365.51 ms`, `soft_count=0`), and the taskset repeat `batch-1783702255-300` failed all-pass (`298/300`, `soft_count=0`).
- Rejected for default: whole-batch RX session reuse (`ANALOG_RX_BATCH_SESSION_MAX_IMAGES=0`) and RX CAPTURE preconnect on the combined profile. Both stayed all-pass at 50 images, but p95 worsened versus the bounded no-preconnect candidate.
- Done but not default at that earlier checkpoint: `ANALOG_REMOTE_DECODED_FORMAT=npy`, tmpfs decoded-output placement, remote-dir soft completion, and minimal worker response. The 7/11 candidate has since promoted `.npy`, minimal/summary-only response, and persistent-probe soft completion at `0.05`; tmpfs remains diagnostic-only.
- In progress: rare-stall diagnosis after fast-first. The runner now reports `rx_arm_ms`, `rx_wait_ms`, `remote_decode_queue_ms`, `remote_decode_reported_ms`, `remote_decode_restart_ms`, and opt-in board stall snapshot paths; the newest 300-image evidence shows the normal path is fast enough, but recovered no-sync/low-sync retries still dominate max latency.
- In progress: overlap-safe IQ optimization. `ANALOG_PIPELINE_RF_DECODE_OVERLAP` now separates guarded depth 2 from the old aggressive overlap mode in code and in Cockpit runtime arguments. Validation showed guarded depth 2 can be all-pass after the WAIT control-timeout margin, but median and p95 are worse than the zero-delay depth-1 profile, so it is not recommended.
- In progress: decode-worker response tail. The worker now ignores stale responses by `summary_json`/`request_id`, and `ANALOG_REMOTE_DECODE_RESPONSE_MODE=minimal` cuts the stdout payload to about `731 bytes` per image. The remaining max tails are now mostly true board write/decode stalls on `/home` or RX arm/capture stalls on tmpfs; minimal response remains opt-in until repeat evidence beats the current default on median, p95, and wall.
- Deferred: Task 8 security-on overhead. Keep performance numbers security-off until IQ transport p95 is stable; then measure ML-KEM/auth as a separate delta.

## File Map

- Modify: `USRP292x/RunAnalogLatentBatch.py`
  - Owns IQ direct batch execution, persistent TX/RX calls, `RemoteAnalogDecodeWorker`, remote-dir summaries, retry records, and per-image transport timing.
- Modify: `USRP292x/OtaRxPersistentServer.cpp`, `USRP292x/OtaTxPersistentServer.cpp`
  - Own persistent USRP control sockets. Multi-command sessions are supported for experiments, but the recommended cockpit profile still uses the existing one-command request shape.
- Modify: `USRP292x/AnalogLatentLink.py`
  - Owns IQ frame make/decode CLI and `decode-server`. Limit edits here to decode timing, sync-path selection, and cacheable decode setup.
- Modify: `USRP292x/test_analog_latent_link.py`
  - Unit tests for IQ runner, remote-dir behavior, worker startup, summary aggregation, and retry metadata.
- Modify: `Semantic-Communication/session_bootstrap/demo/openamp_control_plane_demo/usrp_runtime.py`
  - Owns cockpit runtime selection between QPSK and IQ direct, runner arguments, remote-dir handling, and transport benchmark extraction.
- Modify: `Semantic-Communication/session_bootstrap/demo/openamp_control_plane_demo/server.py`
  - Owns `/api/start-batch-inference`, `/api/batch-state`, system status hydration, and cockpit-visible batch progress.
- Modify: `Semantic-Communication/session_bootstrap/demo/openamp_control_plane_demo/tests/test_server.py`
  - Backend tests for cockpit state, runner selection, IQ remote-dir, and benchmark display updates.
- Modify only after a completed measurement: `INTEGRATION_STATUS.md` and `docs/cockpit_usrp_qpsk_tvm_runbook.md`
  - Record command, batch id, transport summary, TVM summary, and any security-on overhead.
- Do not modify during IQ optimization: `USRP292x/RunQpskFileBatchSpoolArq.py` unless an existing test cannot pass because a shared contract changed.

## Execution Plan

### Task 1: Preserve The Known-Good IQ/QPSK Boundary

**Files:**
- Modify: `USRP292x/test_analog_latent_link.py`
- Modify: `Semantic-Communication/session_bootstrap/demo/openamp_control_plane_demo/tests/test_server.py`
- Inspect only: `USRP292x/RunQpskFileBatchSpoolArq.py`

**Interfaces:**
- Consumes: `current_jscc_link_mode()`, `LINK_MODE_IQ_DIRECT`, `LINK_MODE_QPSK`.
- Produces: regression tests that fail if cockpit routes IQ direct to the QPSK runner or if remote-dir behavior disappears.

- [ ] Add a test proving `JSCC_LINK_MODE=iq-direct` selects `USRP292x/RunAnalogLatentBatch.py` and sets `--remote-decode-result-mode remote-dir`.
- [ ] Add a test proving `JSCC_LINK_MODE=qpsk` still selects `USRP292x/RunQpskFileBatchSpoolArq.py`.
- [ ] Add a test proving IQ remote-dir does not restage local wire blobs and does not upload decoded outputs back to the board.
- [ ] Run:

```powershell
python -m pytest USRP292x/test_analog_latent_link.py -q
python -m pytest Semantic-Communication/session_bootstrap/demo/openamp_control_plane_demo/tests/test_server.py -q
git diff -- USRP292x/RunQpskFileBatchSpoolArq.py
```

Expected result: tests pass; the QPSK runner diff is empty.

- [ ] Commit:

```powershell
git add USRP292x/test_analog_latent_link.py Semantic-Communication/session_bootstrap/demo/openamp_control_plane_demo/tests/test_server.py
git commit -m "test: guard iq direct runner selection"
```

### Task 2: Add IQ Stage Metrics Without Behavior Changes

**Files:**
- Modify: `USRP292x/RunAnalogLatentBatch.py`
- Modify: `Semantic-Communication/session_bootstrap/demo/openamp_control_plane_demo/usrp_runtime.py`
- Modify: `USRP292x/test_analog_latent_link.py`

**Interfaces:**
- Consumes: existing per-image `records` and `batch_spool_summary.json`.
- Produces: `iq_stage_benchmark` in the runner summary with median, mean, p95, max, and count for `tx_control_ms`, `rx_arm_ms`, `rx_capture_ms`, `rx_wait_ms`, `remote_decode_queue_ms`, `remote_decode_ms`, `remote_dir_publish_ms`, `retry_wait_ms`, and `total_transport_ms`.

- [ ] Write failing tests with a synthetic summary containing three image records. Assert that `_transport_benchmark_from_iq_round_records()` returns stage-level medians and p95 values.
- [ ] In `RunAnalogLatentBatch.py`, record timestamps around:
  - RX arm/control request.
  - TX send/control request.
  - RX capture completion.
  - remote decode worker request/response.
  - remote-dir publish path returned by `decode-server`.
  - retry delay and retry attempt count.
- [ ] Keep the existing summary keys unchanged. Add new keys instead of renaming old keys.
- [ ] In `usrp_runtime.py`, pass `iq_stage_benchmark` through to Cockpit status without changing the current `transport_benchmark` shape.
- [ ] Run:

```powershell
python -m pytest USRP292x/test_analog_latent_link.py -q
python -m pytest Semantic-Communication/session_bootstrap/demo/openamp_control_plane_demo/tests/test_server.py::TestUsrpRuntimeBenchmark -q
```

Expected result: tests pass; old benchmark fields still exist.

- [ ] Commit:

```powershell
git add USRP292x/RunAnalogLatentBatch.py USRP292x/test_analog_latent_link.py Semantic-Communication/session_bootstrap/demo/openamp_control_plane_demo/usrp_runtime.py
git commit -m "perf: expose iq direct stage metrics"
```

### Task 3: Keep Board Decode Warm And Verify It Is Not Restarting Per Image

**Files:**
- Modify: `USRP292x/RunAnalogLatentBatch.py`
- Modify: `USRP292x/AnalogLatentLink.py`
- Modify: `USRP292x/test_analog_latent_link.py`

**Interfaces:**
- Consumes: `RemoteAnalogDecodeWorker.start()` and `AnalogLatentLink.py decode-server`.
- Produces: a single long-lived board decode process per batch, with `ANALOG_DECODE_PIPELINE_WARMUP=1` recorded in the batch summary.

- [ ] Add a test that starts a fake `RemoteAnalogDecodeWorker` and sends three decode requests. Assert that worker startup count is `1`.
- [ ] Add a test that the worker environment includes `OMP_NUM_THREADS=1`, `OPENBLAS_NUM_THREADS=1`, and `ANALOG_DECODE_PIPELINE_WARMUP=1`.
- [ ] In `decode-server`, print one JSON `ready` line after imports, sync setup, and handwritten decode setup are loaded.
- [ ] In `RemoteAnalogDecodeWorker.start()`, wait for the `ready` line before starting batch timing. If the ready line is not received inside the existing startup timeout, fail before the first image.
- [ ] Run:

```powershell
python -m pytest USRP292x/test_analog_latent_link.py::test_remote_decode_worker_uses_pipeline_warmup -q
python -m pytest USRP292x/test_analog_latent_link.py::test_remote_decode_worker_remote_dir_mode -q
```

Expected result: worker startup is outside per-image timing and remote-dir behavior stays active.

- [ ] Commit:

```powershell
git add USRP292x/RunAnalogLatentBatch.py USRP292x/AnalogLatentLink.py USRP292x/test_analog_latent_link.py
git commit -m "perf: keep iq decode worker ready"
```

### Task 4: Pipeline Remote Decode And Board TVM Consumption (Implemented As Opt-In)

**Files:**
- Modify: `USRP292x/RunAnalogLatentBatch.py`
- Modify: `Semantic-Communication/session_bootstrap/demo/openamp_control_plane_demo/server.py`
- Modify: `Semantic-Communication/session_bootstrap/demo/openamp_control_plane_demo/tests/test_server.py`
- Modify: `USRP292x/test_analog_latent_link.py`

**Decision after measurement:** keep this path behind `OPENAMP_IQ_STREAMING_TVM=1` or `USRP_IQ_STREAMING_TVM=1`. The code and tests are useful for future pipeline experiments, but the 2026-07-10 300-image run showed board contention when TVM consumes decoded files while IQ decode is still active. The default cockpit path should remain sequential: finish IQ transport/decode first, then run big.LITTLE TVM.

**Interfaces:**
- Consumes: remote decoded `.npz` files published under `/home/user/cockpit_usrp_rx/...`.
- Produces: cockpit batch state that marks an image ready for TVM as soon as its remote decoded path is available, instead of waiting for the whole transport batch to finish.

- [ ] Add a unit test with three synthetic remote-dir decode summaries. The first summary appears before the second RX finishes. Assert that `server.py` exposes image `0` as TVM-ready while images `1` and `2` are still in transport.
- [ ] Add a runner test proving `RunAnalogLatentBatch.py` writes each image summary immediately after decode response, not only at batch end.
- [ ] Add a small state object in `server.py` for streaming IQ remote-dir results:
  - `pending`: transport started, no decoded path yet.
  - `decoded`: board decoded `.npz` path exists.
  - `tvm_running`: handwritten operator is consuming the path.
  - `done`: reconstruction result has been hydrated into batch state.
  - `failed`: decode or TVM failed for that image.
- [ ] Keep the existing batch completion API response stable. New fields must be additive.
- [ ] Run:

```powershell
python -m pytest Semantic-Communication/session_bootstrap/demo/openamp_control_plane_demo/tests/test_server.py::TestBatchInference -q
python -m pytest USRP292x/test_analog_latent_link.py -q
```

Expected result: Cockpit can update reconstructed image output before the final transport summary is written.

- [ ] Commit:

```powershell
git add Semantic-Communication/session_bootstrap/demo/openamp_control_plane_demo/server.py Semantic-Communication/session_bootstrap/demo/openamp_control_plane_demo/tests/test_server.py USRP292x/RunAnalogLatentBatch.py USRP292x/test_analog_latent_link.py
git commit -m "perf: pipeline iq remote decode into tvm"
```

### Task 5: Add RX/TX Double Buffering For IQ Direct

**Files:**
- Modify: `USRP292x/RunAnalogLatentBatch.py`
- Modify: `USRP292x/test_analog_latent_link.py`

**Interfaces:**
- Consumes: persistent RX server on `RX_CONTROL_HOST:RX_CONTROL_PORT` and persistent TX server on `TX_CONTROL_HOST:TX_CONTROL_PORT`.
- Produces: `ANALOG_PIPELINE_DEPTH=2`, allowing image `n+1` RX to arm while image `n` decode request is in flight.

- [ ] Add CLI/env support:

```text
--pipeline-depth
ANALOG_PIPELINE_DEPTH
```

Default is `1`. Cockpit fast IQ profile may set `2` after the tests pass.

- [ ] Add tests for depth `1` and depth `2` using fake RX/TX control clients. Assert depth `2` does not reuse an RX output path until the previous decode response has been collected.
- [ ] Implement a two-slot ring:
  - slot `0`: RX output path, TX input path, image index, attempt number.
  - slot `1`: same fields.
  - a slot is free only after decode response and summary write have completed.
- [ ] Preserve retry behavior. A failed image retries in its current slot before the next image can claim that slot.
- [ ] Add `pipeline_depth`, `max_inflight`, and `slot_wait_ms` to `batch_spool_summary.json`.
- [ ] Run:

```powershell
python -m pytest USRP292x/test_analog_latent_link.py -q
```

Expected result: depth `1` keeps current ordering; depth `2` overlaps control waits without changing output filenames.

- [ ] Commit:

```powershell
git add USRP292x/RunAnalogLatentBatch.py USRP292x/test_analog_latent_link.py
git commit -m "perf: add iq direct double buffering"
```

### Task 6: Add Fast Sync First, Fallback Sync Second

**Files:**
- Modify: `USRP292x/AnalogLatentLink.py`
- Modify: `USRP292x/RunAnalogLatentBatch.py`
- Modify: `USRP292x/test_analog_latent_link.py`

**Interfaces:**
- Consumes: current IQ sync options such as `ANALOG_ROBUST_SYNC`, `ANALOG_SYNC_CANDIDATES`, and `ANALOG_SYNC_SEARCH_WINDOW_SYMBOLS`.
- Produces: `ANALOG_SYNC_PROFILE=fast-first`, where normal frames use a short search and only failed frames retry with the slower path.

- [x] Add CLI/env support:

```text
--sync-profile fast-first
ANALOG_SYNC_PROFILE=fast-first
ANALOG_FAST_SYNC_CANDIDATES=4
ANALOG_FAST_SYNC_SEARCH_WINDOW_SYMBOLS=1024
ANALOG_FALLBACK_SYNC_CANDIDATES=12
ANALOG_FALLBACK_SYNC_SEARCH_WINDOW_SYMBOLS=4096
```

- [x] Add a decode test with a fake sync function returning low metric on the fast pass and success on the fallback pass. Assert the summary contains `sync_profile=fast-first`, `sync_pass=2`, and both metric values.
- [x] Keep current default behavior when `ANALOG_SYNC_PROFILE` is absent.
- [x] Add per-pass decode timings to the image summary: `fast_sync_ms`, `fallback_sync_ms`, and `selected_sync_metric`.
- [x] Run:

```powershell
python -m pytest USRP292x/test_analog_latent_link.py -q
```

Expected result: existing tests pass; new fallback test proves no successful current frame is forced through the slow path.

- [ ] Commit:

```powershell
git add USRP292x/AnalogLatentLink.py USRP292x/RunAnalogLatentBatch.py USRP292x/test_analog_latent_link.py
git commit -m "perf: add fast first iq sync"
```

### Task 7: Cockpit Button Validation, 20 Images Then 300 Images

**Files:**
- Modify after measurement: `INTEGRATION_STATUS.md`
- Modify after measurement: `docs/cockpit_usrp_qpsk_tvm_runbook.md`

**Interfaces:**
- Consumes: Cockpit one-click batch behavior, board decode-server, persistent TX/RX.
- Produces: documented batch ids and a before/after table.

- [x] Fully stop existing Cockpit/backend/USRP helper processes before testing.
- [x] Restart via Docker where Linux/bash is needed. Do not use WSL.
- [x] On the board, activate the virtual environment before starting board-side Python helpers:

```bash
source /home/user/venv/bin/activate
```

- [x] Confirm runtime mode before the run:

```powershell
# Expected: transport_mode=usrp, jscc_link_mode=iq-direct, connection_ready=true
Invoke-RestMethod http://127.0.0.1:8000/api/system-status | ConvertTo-Json -Depth 8
```

- [x] Trigger a 20-image run using the same backend endpoint used by the Cockpit Desktop test button.
- [x] Accept the 20-image run only if:
  - All images pass.
  - Cockpit batch state updates reconstruction output.
  - `transport_benchmark` and `iq_stage_benchmark` are present.
  - TVM median remains in the `235-255 ms` band.
- [x] Trigger a 300-image run using the Cockpit button behavior.
- [x] Accept the 300-image run only if:
  - `300/300`, fail `0`.
  - IQ transport median below `200 ms`.
  - IQ transport p95 below `450 ms`.
  - Reconstruction comparison updates during or immediately after the run.
  - No QPSK files changed.
- [ ] Append the result table to the docs and commit:

```powershell
git add INTEGRATION_STATUS.md docs/cockpit_usrp_qpsk_tvm_runbook.md
git commit -m "docs: record iq direct optimization results"
```

### Task 8: Measure Authentication And ML-KEM Overhead Separately

**Files:**
- Modify after measurement: `INTEGRATION_STATUS.md`
- Modify after measurement: `docs/cockpit_usrp_qpsk_tvm_runbook.md`
- Modify only if needed: `mlkem_link/` tests or config helpers.

**Interfaces:**
- Consumes: current security config with `auth_enabled=true`, `sig_policy=DUAL_REQUIRED`, and disabled runtime channel.
- Produces: two documented runs: security-off performance baseline and security-on overhead measurement.

- [ ] Keep the optimized IQ run with security off as the performance baseline.
- [ ] Enable the security channel for a small smoke run first. Confirm the handshake is session-level, not per image.
- [ ] Run a 20-image security-on batch. Accept only if:
  - All images pass.
  - No per-image ML-KEM handshake appears in logs.
  - Median transport overhead is recorded.
- [ ] Run a 300-image security-on batch only after the 20-image run passes.
- [ ] If security-on overhead is above `5%` after session setup, keep the performance profile security-off and document the measured delta instead of hiding it.
- [ ] Commit:

```powershell
git add INTEGRATION_STATUS.md docs/cockpit_usrp_qpsk_tvm_runbook.md
git commit -m "docs: record iq direct security overhead"
```

### Task 9: Diagnose Remaining RX/Decode Max Outliers

**Files:**
- Modify only if needed: `USRP292x/RunAnalogLatentBatch.py`
- Modify only if needed: `USRP292x/AnalogLatentLink.py`
- Modify after measurement: `plan_20260710.md`, `INTEGRATION_STATUS.md`, `docs/cockpit_usrp_qpsk_tvm_runbook.md`

**Interfaces:**
- Consumes: `iq_stage_benchmark`, per-image `decode_summary.json`, RX/TX persistent server logs, and board decode worker logs.
- Produces: either a confirmed environmental stall record or a targeted fix for RX capture/decode scheduling.

- [x] Extract top 10 `total_transport_ms`, `rx_capture_ms`, `remote_decode_ms`, and `fast_sync_ms` records from the fast-first 300-image run.
- [x] Correlate largest RX outliers with RX/TX server command logs; this separated true RX `WAIT` stalls from runner-side/control overhead.
- [x] Add opt-in board CPU/IO stall snapshots for high `rx_wait_ms`, `rx_capture_ms`, `remote_decode_ms`, queue, publish, or error records. Use `ANALOG_REMOTE_STALL_SNAPSHOT=1`, `ANALOG_REMOTE_STALL_SNAPSHOT_THRESHOLD_SEC=1.0`, and `ANALOG_REMOTE_STALL_SNAPSHOT_LIMIT=3`.
- [x] In the next live run, enable the snapshot switch and collect board CPU/IO status near high `rx_wait_ms` or `remote_decode_ms` outliers. Result: `batch-1783641924-50` captured three snapshots and identified a true RX wait stall followed by queue propagation.
- [x] Fix the snapshot mechanism to launch asynchronously. Synchronous SSH snapshots blocked the single decode-worker finalization path and created artificial multi-second `remote_decode_queue_ms` outliers.
- [x] Rerun a 50-image stall-snapshot batch after the async fix. Result: `batch-1783642466-50` stayed `50/50`, no snapshot fired because no record crossed `1.0 s`, and `remote_decode_queue_ms` max stayed `92.41 ms` instead of inheriting snapshot wall time.
- [x] Run a 300-image async snapshot diagnostic. Result: `batch-1783642640-300` stayed `300/300`, TVM p95 `243.97 ms`, transport median `190.29 ms`, but p95 `946.07 ms`; long tail is now confirmed as mixed RX wait/capture and decode-worker request stalls, not QPSK or remote-dir publish I/O.
- [x] Add opt-in `ANALOG_RX_WAIT_TIMEOUT_SEC` so RX WAIT can fail fast and rely on the existing ARQ retry path when the capture server would otherwise wait several seconds.
- [x] Validate `ANALOG_RX_WAIT_TIMEOUT_SEC=0.5` on 50 images. Result: `batch-1783643493-50` stayed `50/50`, transport p95 `322.94 ms`, and `rx_wait_ms` max `55.34 ms`; keep opt-in until a 300-image run validates reliability and TVM p95.
- [x] Validate `ANALOG_RX_WAIT_TIMEOUT_SEC=0.5` on 300 images. Result: failed reliability gate (`284/300`, fallback `16`) due `WAIT timeout` followed by `capture_already_running`; keep unset.
- [ ] Design RX timeout recovery around explicit capture cancel/drain before retrying a timed-out RX WAIT.
- [x] Determine whether current known stalls happen before decode request, inside RX capture, inside sync correlation, or during remote-dir publish.
- [x] Add `remote_decode_queue_ms` so double-buffer queue wait is measured separately from RX capture and remote decode execution.
- [x] Add additive `rx_arm_wall_sec` and `rx_wait_wall_sec` records, exposed as `rx_arm_ms` and `rx_wait_ms`, so the next run separates control-command overhead from real RX waiting.
- [x] Add opt-in `ANALOG_RETRY_ON_BURST_MISS=1` to skip full sync/fallback when burst detection explicitly reports `burst threshold not crossed`.
- [x] Validate the opt-in guard on 50 images. Result: it removed the slow burst-miss decode/fallback path but did not solve RX capture p95, so it is not the recommended default yet.
- [x] Run a cockpit-button-equivalent 50-image IQ batch with `rx_arm_ms` and `rx_wait_ms`. Result: `batch-1783629764-50` showed stable `rx_wait_ms` and meaningful fixed control overhead, so prioritize control command overhead before RX capture tuning.
- [x] Test Python-side `ANALOG_PRECONNECT_CONTROL=1` on 50 images. Result: `batch-1783631315-50` improved p95 and max modestly but did not improve median, so keep it opt-in.
- [x] Add C++ same-connection multi-command support to the persistent RX/TX servers and expose opt-in `ANALOG_RX_SESSION_CONTROL=1` in the IQ runner.
- [x] Validate RX same-session control on 50 images. Result: `batch-1783632169-50` stayed all-pass but worsened transport median and p95, so keep it opt-in and unset in the cockpit runbook.
- [x] Add opt-in low-sync early retry so a fast-first frame below a configured metric can request a fresh capture before fallback decode, but only while another ARQ attempt remains.
- [x] Validate low-sync early retry. Result: 50-image threshold `0.05` looked stable, but 300-image `batch-1783633910-300` failed `299/300`; keep it opt-in and unset.
- [x] Add `remote_decode_reported_ms` beside runner-side `remote_decode_ms` so future logs can distinguish board decode compute time from decode-worker wait/stall time.
- [x] Add opt-in decode-worker request timeout and restart-on-timeout handling for both sequential and pipeline IQ paths.
- [x] Validate decode-worker timeout/restart on a short live run before trying 300 images. Result: `batch-1783635469-50` stayed all-pass, but no timeout fired.
- [x] Validate decode-worker timeout/restart on 300 images. Result: `batch-1783635568-300` stayed all-pass and triggered 3 timeouts, but p95 and max worsened because restart time and RX stalls still propagated through the depth-2 queue.
- [x] Validate `ANALOG_PIPELINE_DEPTH=1` as a tail-control diagnostic. Result: `batch-1783636039-300` stayed all-pass and lowered p95, but wall time rose to `104.43 s`; keep depth 2 for throughput.
- [x] Validate shorter RX tail. Result: `0.02` failed `28/50`; `0.04` stayed all-pass but worsened median/p95 and retry count. Keep `ANALOG_RX_TAIL_SEC=0.05`.
- [x] Add explicit `remote_decode_restart_wall_sec` / `remote_decode_restart_ms` so timeout records explain the gap between `decode_wall_sec` and `total_wall_sec`.
- [x] Add richer non-timeout failure-stage timings for capture/sync errors that currently fall through minimal sequential error records.
- [x] Add server-side RX control timing fields for `arm_wait`, drain, stream command issue, receive loop, STOP command, and STOP wait, and propagate them into per-image records plus `iq_stage_benchmark`.
- [x] Validate the server-side RX timing fields on a 50-image Cockpit-equivalent run. Result: `batch-1783676258-50` stayed all-pass and showed normal server receive/capture around `64 ms`, consistent with the current `0.05 s` RX tail.
- [x] Re-test shorter RX tail values on the current profile. Result: `0.04` retried in 5-image sanity, and `0.045` passed 50 images but worsened median/p95. Keep `ANALOG_RX_TAIL_SEC=0.05`.
- [x] Prevent failed SSH ControlMaster startup from being retried once per image after a batch-level failure.
- [x] Test a smaller isolated 50-image run with no extra status polling and compare `rx_capture_ms` max. Result: `batch-1783678924-50` showed polling is not the primary cause; server capture stayed around `64 ms` while runner-side wait expanded.
- [x] Add derived benchmark fields for runner/control overhead: `rx_capture_control_overhead_ms` and `remote_decode_response_overhead_ms`.
- [x] Validate the derived overhead fields on a 50-image Cockpit-equivalent run. Result: `batch-1783680558-50` stayed all-pass but showed one recovered no-sync retry and p95 split between RX capture/control overhead and decode response overhead.
- [x] Add STOP/drain cleanup after post-capture decode/no-sync failure before ARQ retry.
- [x] Validate the cleanup on the normal 50-image Cockpit-equivalent path. Result: `batch-1783681389-50` stayed `50/50`, fallback `0`, with IQ median/p95/max `174.91/269.24/347.72 ms`; no retry occurred in this run, so retry-path benefit remains to be verified on the next failure occurrence.
- [x] Run a 300-image Cockpit-equivalent gate after the cleanup. Result: `batch-1783682083-300` stayed `300/300`, fallback `0`, IQ median/p95 `174.42/308.45 ms`, TVM median/p95 `243.38/255.24 ms`, but still produced `305` stage records and multi-second max.
- [x] Fix STOP client timeout budgeting so the runner can wait through the configured 8-second RX drain instead of timing out at 5 seconds.
- [x] Validate the STOP timeout-budget fix on a fresh 300-image gate. Result: `batch-1783682666-300` stayed `300/300`, fallback `0`, but `rx_stop_after_wait_timeout.log` still showed STOP `ERR_TIMEOUT`, revealing the old RX session was kept open while direct STOP tried a new connection.
- [x] Close the RX session before direct STOP on WAIT timeout in both depth-1 and pipeline capture paths.
- [x] Validate the session-before-STOP fix on a fresh 300-image gate. Result: `batch-1783683491-300` stayed `300/300`, fallback `0`, IQ median/p95 `169.48/299.73 ms`, TVM median/p95 `242.38/245.50 ms`; the one retry returned STOP `OK` instead of `ERR_TIMEOUT`.
- [x] Recheck tmpfs decoded-output after RX cleanup. Result: real tmpfs smoke `batch-1783684070-50` capped `write_npz` max at `2.719 ms`, but IQ median/p95 worsened to `179.92/360.48 ms` with one arm-not-ready retry, so tmpfs remains diagnostic-only.
- [x] Add `remote_decode_soft_completed` per-image record field so future soft-completion runs can separate file-probe completions from normal worker stdout responses.
- [x] Add decode-side timing aggregation for matched filter, initial sync, CFO estimate, payload recovery, latent reconstruct, `.npz` write, and wire/summary stages.
- [x] Run a fresh 50-image Cockpit-equivalent timing smoke with the new fields. Result: `batch-1783686020-50`, `50/50`, fallback `0`, IQ median/p95 `171.80/324.63 ms`, TVM median/p95 `239.71/250.01 ms`; decode internals were bounded while RX control and decode response overhead remained the bigger tails.
- [x] Preserve failed remote-decode wall time in error records so low-sync/no-sync retry attempts are not reported as `decode_wall_sec=0`.
- [x] Run a fresh 300-image Cockpit-equivalent timing gate with the new fields. Result: `batch-1783686930-300`, `300/300`, fallback `0`, IQ median/p95 `166.04/288.97 ms`, TVM median/p95 `240.29/242.87 ms`; remaining max was one `/home` `.npz` write stall plus several RX arm/capture and decode response tails.
- [x] Add opt-in sequential batch RX control-session reuse behind `ANALOG_RX_BATCH_SESSION_CONTROL=1`. The runner now opens one RX control connection for non-pipeline sequential batches, passes it through `args.rx_control_session`, closes it at batch end, and clears it after WAIT timeout/capture-busy failures so a dead socket is not reused. Local validation: `python -m pytest USRP292x/test_analog_latent_link.py -q` -> `98 passed`; `python -m py_compile USRP292x\RunAnalogLatentBatch.py USRP292x\AnalogLatentLink.py`; `git diff --check`; QPSK diff empty.
- [x] Validate `ANALOG_RX_BATCH_SESSION_CONTROL=1` on a Cockpit-equivalent 50-image run. Result after cleanup fix: `batch-1783690852-50`, `50/50`, fallback `0`, TVM median/p95 `242.49/246.53 ms`, IQ median/p95/max `155.30/217.47/583.30 ms`, `rx_batch_session_control_enabled=true`, stage records `50`.
- [x] Validate `ANALOG_RX_BATCH_SESSION_CONTROL=1` on a fresh 300-image gate. Result after cleanup fix: `batch-1783690925-300`, `300/300`, fallback `0`, TVM median/p95 `243.80/246.95 ms`, IQ median/p95/max `162.20/321.68/6828.85 ms`, stage records `301`. Do not promote as default because p95 remains above TVM and above the no-batch A/B.
- [x] Run no-batch A/B on the same code/profile with `ANALOG_RX_BATCH_SESSION_CONTROL=0`. Result: `batch-1783691290-300`, `300/300`, fallback `0`, TVM median/p95 `241.84/253.13 ms`, IQ median/p95/max `172.71/301.74/8598.55 ms`, `rx_batch_session_control_enabled=false`. Conclusion: keep batch session opt-in only; next work should target RX arm/wait/control stalls and rare `/home` `.npz` write stalls.
- [x] Reject `PERSISTENT_RX_TX_DELAY=0.005` on current no-batch profile. 50-image smoke `batch-1783691806-50` stayed `50/50`, fallback `0`, but IQ median/p95 worsened to `202.47/306.92 ms` versus the 0-delay 50-image runs around `155-172/217-228 ms`. Keep `PERSISTENT_RX_TX_DELAY=0`.
- [x] Preserve STOP timing after WAIT timeout cleanup. WAIT timeout error text can contain stale `stop_cmd_sec=0` / `stop_wait_sec=0`; the runner no longer reparses that same error after STOP succeeds, so stage records keep the real `rx_server_stop_*` fields from `rx_stop_after_wait_timeout.log`. Local validation: targeted WAIT/session tests passed; full `python -m pytest USRP292x/test_analog_latent_link.py -q` -> `99 passed`; `python -m py_compile USRP292x\RunAnalogLatentBatch.py USRP292x\AnalogLatentLink.py`.
- [x] Add opt-in `ANALOG_REMOTE_DECODE_RESPONSE_ONLY_SUMMARY=1` / `--remote-decode-response-only-summary` for remote-dir persistent-worker mode. It skips the board-side `decode_summary.json` write and reconstructs the local summary from the worker response. 50-image Cockpit-equivalent smoke `batch-1783693518-50` stayed `50/50`, fallback `0`, `remote_decode_response_only_summary=true`, TVM median/p95 `243.64/245.89 ms`, IQ median/p95/max `165.09/382.82/583.99 ms`. Do not promote as default: p95 worsened, with `rx_capture_control_overhead_ms` p95 `237.85 ms` and `remote_decode_response_overhead_ms` p95 `124.65 ms`.
- [x] A/B decoded output placement/format for the rare decoded-output stall without changing QPSK. Result: the `/tmp` decoded-output 50-image smoke improved p95, but the follow-up 300-image runs either failed the gate before the STOP recovery fix or stayed all-pass with worse p95 (`batch-1783735946-300`, IQ p95 `321.45 ms`). Keep the stable `/home/user/cockpit_usrp_rx` decoded-output directory for Cockpit.
- [x] A/B shorter soft-completion (`ANALOG_REMOTE_DECODE_SOFT_COMPLETE_SEC=0.02`) on the current profile. Result: valid 50-image smoke looked good, but the 300-image gate `batch-1783736949-300` landed at IQ p95 `279.31 ms`, above both TVM p95 and the accepted `0.05` baseline. Keep `0.05`.
- [x] A/B RX batch-session shape on the current profile. Result: window `32` and no-batch both worsened 50-image p95 versus the accepted window `16` profile. Keep `ANALOG_RX_BATCH_SESSION_CONTROL=1` with `ANALOG_RX_BATCH_SESSION_MAX_IMAGES=16`.
- [x] Harden STOP recovery after `ERR_TIMEOUT` by polling `STATUS` through the configured drain deadline. This is a reliability fix for retry cleanup, not a default performance-profile change.
- [x] Run offline tail audit on `batch-1783730551-300`. Result: `33/300` records exceeded TVM p95; the over-threshold set splits between RX control/response overhead and decode-worker/soft-completion wait, while decoded-output write is no longer the dominant p95 factor.
- [x] Add automatic `iq_tail_audit` to future `batch_spool_summary.json` files so the next hardware A/B can compare tail classes without ad hoc log parsing.
- [x] Expose `iq_tail_audit` through the USRP wrapper, backend batch/crypto status, and Cockpit security panel.
- [x] Freeze the next-resume experiment order while paused: RX control-plane timing first, soft-completion threshold second, publish-event/worker protocol third, decoded-output placement last.
- [x] Design RX capture health handling for multi-second `rx_wait_ms` / `rx_capture_ms` stalls. Implemented as opt-in shared RX control-session recycling after successful-but-slow RX records, with summary counters for hardware A/B.
- [ ] Keep `ANALOG_SYNC_PROFILE=fast-first` as the recommended setting unless a repeat 300-image run loses all-pass reliability.
- [ ] Do not re-enable streaming TVM as a default optimization. It is a separate contention experiment.

## Validation Commands

Use these before claiming the IQ optimization is ready:

```powershell
git status -sb --untracked-files=all
python -m pytest USRP292x/test_analog_latent_link.py -q
python -m pytest Semantic-Communication/session_bootstrap/demo/openamp_control_plane_demo/tests/test_server.py -q
npm run typecheck --prefix Semantic-Communication/cockpit_desktop
git diff --check
```

Use this guard before and after each IQ task:

```powershell
git diff -- USRP292x/RunQpskFileBatchSpoolArq.py
```

Expected output is empty.

## Rollback Plan

- If IQ direct loses the `252 ms` visible reconstruction path, revert only the last IQ optimization commit and rerun the 20-image cockpit-button test.
- If p95 worsens while median improves, keep the stage-metric commit and revert the pipeline or sync commit that caused the long tail.
- If QPSK changes appear in `git diff`, stop and split the shared-interface fix from any QPSK behavior change.
- If security-on work affects transport latency, keep it behind the security toggle and leave the default IQ performance profile unchanged.

## Cleanup Rules

- Keep committed docs, tests, and source changes.
- Remove or ignore generated local artifacts: cockpit logs, `runtime_logs/`, `USRP292x/analog_latent_runs/`, `usrp_latent_input/`, board backups, and `nul` files.
- Keep only the batch ids and summary numbers in docs unless a raw log is needed as evidence for a failing test.
- After cleanup, `git status -sb --untracked-files=all` must show no modified or untracked files except intentionally staged work.

## Execution Notes

- Start with Task 1 and Task 2 before changing timing behavior. They make the next optimization measurable.
- Try `ANALOG_PIPELINE_DEPTH=2` only after remote-dir streaming is correct.
- Do not tune sync thresholds from a single image. Use at least a 20-image smoke before the 300-image run.
- Treat 300-image all-pass as the release gate. Single-image speed is useful for diagnosis but not enough for the competition path.
