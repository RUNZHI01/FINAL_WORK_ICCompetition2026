# Project dependencies — IC Competition 2026 Phytium track
# Usage:  source .venv/bin/activate && pip install -r requirements.txt
# Docker: see docker/Dockerfile — Tongsuo is built inside the container
#
# ── Not pip-installable (need source / manual build) ──
#   tvm (apache-tvm-ffi)  — TVM inference runtime, built from source
#   tongsuo                — C library, Docker builds it → tongsuo-dist/

# ── Core crypto (mlkem_link) ──
cryptography>=46.0          # AES-256-GCM / SM4-128-GCM AEAD  (docs pin 46.0.6)
liboqs-python>=0.14.0       # ML-KEM fallback backend         (docs pin 0.14.0)

# ── Scripts / TUI ──
numpy>=2.0
Pillow>=12.0
textual>=8.0
rich>=14.0
matplotlib>=3.8

# ── Testing ──
pytest>=9.0
