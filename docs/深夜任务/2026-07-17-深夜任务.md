# 2026-07-17-深夜任务

***！！！每次进入下一个目标前，请确保你已经完全理解该目标的含义，以防误操作！！！***

## 1. FIT 运行时间尽可能降低

## 2. cockpit desktop 进度条配色调整

* **步骤 1（图片处理）** ：浅蓝 / 灰蓝色 (e.g., `#90CAF9`)
* **步骤 2（USRP 传输）** ：中蓝色 (e.g., `#42A5F5`)
* **步骤 3（TVM 推理）** ：深蓝 / 品牌主蓝 (e.g., `#1E88E5` 或采用图中的按钮蓝)

## 3. 故障测试族扩展

- 参照 docs/深夜任务/ChatGPT-FIT测试数量与关系.pdf
- FIT 04-06：先补 UI，不用接入真机功能，先把 UI 做出来再说
- S-FIT-01 ~ S-FIT-07：同样先补 UI，风格与 FIT 01-06 统一，不过我记得好像是实现了的，如果实现了的话尝试接入，没实现的话跳过接入只补 UI

## 4. 本次重建对比图 demo 扩展

- USRP QPSK 模式下补充原图和 psnr ssim 计算

## 5. 尝试关掉 10 张预热，看对性能影响大不大，尽量删掉对性能影响不大的预热环节，因为演示的时间很紧张

## 6. 根据当前源代码，在本文件夹下新建文件 文档交接.md，讲清楚 加密、认证 和 USRP链路 两个部分分别怎么在一页PPT中精要简练地展示出核心功能与优势，该任务可多迭代几轮修改措辞。原文档可参照 E:\Main\Career\集创赛\集创赛(1) 中的 pptx 和 pdf

## 7. commit push 并同步到 main

## 8. 原始图像只保留一开始的 5000 张和最好的 300 张，且要将最好的 100 张放到前 100 张里，然后删掉其他 原始图像*/ 开头的文件夹防止混淆。

## 9. 完成上述任务后，跑一轮所有功能的回归测试，图片处理除了 QPSK 走 20 张，其他的都走 300 张

## 完成记录（2026-07-18）

- [x] FIT-01～03 真机入口压缩到约 6～13 秒；FIT-04～06 完成交互式仿真回放，明确标注未接板端。
- [x] S-FIT-01～07 完成统一 UI，并接入上位机隔离 Docker 自检。
- [x] 三阶段进度色调整为 `#90CAF9`、`#42A5F5`、`#1E88E5`。
- [x] QPSK 重建对比按发送 manifest 找回原图并计算 PSNR/SSIM。
- [x] 评估关闭预热：冷启动被转移到首次点击，故保留默认 10 张隐藏预热。
- [x] 完成 `文档交接.md`。
- [x] 仅保留原始 5000 张与最终 Top300，并用顺序清单保证最佳 100 张优先。
- [x] 完成预录 TVM/MNN/PyTorch 各 300、QPSK 20、IQ 300 回归，详见 `回归测试报告_20260718.md`。
- [x] 推送 `perf/iq-demo-hotpath` 并以快进方式同步 `main`。
