# 2026-07-18 全链路回归报告

## 结论

五条图片链路均完成规定规模且无 fallback。TVM handwritten + big.LITTLE 的 300 张预录中位数为 `243.40 ms`，USRP IQ 直传后 TVM 中位数为 `241.79 ms`，已恢复约 250 ms 的目标水平。

| 链路 | 规模 | 结果 | 核心指标 |
| --- | ---: | --- | --- |
| 预录 TVM | 300 | 300/300 | TVM median `243.40 ms`，mean `261.24 ms` |
| 预录 MNN | 300 | 300/300 | inference median `200.73 ms`，端到端 median `371.85 ms` |
| 预录 PyTorch | 300 | 300/300 | 末张推理 `498.75 ms`，装载 `403.32 ms` |
| USRP QPSK + TVM | 20 | 20/20 | transport median `4975.55 ms`，TVM median `240.72 ms` |
| USRP IQ + TVM | 300 | 300/300 | 空口 median `9.63 ms`，传输/解码 median `248.49 ms`，TVM median `241.79 ms` |

IQ 批次墙钟为 `534.80 s`，其中约 `215 s` 为 Windows Docker TX host-network 控制端口不可达的故障等待。改用 bridge 并直接映射 `127.0.0.1:29221` 后，当前任务从 0 恢复到 300/300；修复后 OTA 开始至 TVM 完成约 `226 s`。该故障墙钟不作为正常性能指标。

## FIT 与 S-FIT

- FIT-01 错误 SHA：真机注入成功，约 `7.00 s`。
- FIT-02 非法参数：真机注入成功，约 `6.07 s`。
- FIT-03 心跳超时：板端当前语义未返回预期 watchdog 结果，`12.61 s` 后进入回放兜底；UI 明确标识。
- FIT-04～06：仅 UI 预留，未宣称真机接入。
- S-FIT-01～07：隔离的上位机 Docker 自检全部通过，单项约 `1.17～1.95 s`；不是板端真机故障注入。

原始 API 状态保存在同目录 `regression_*.json`。回归时安全信道默认启用，实测一次冷握手约 `752 ms`，后续任务复用会话。

## 启动与样本检查

- Windows TX Docker 默认改为 bridge 网络；原生 Linux 仍默认 host network。
- 保留 10 张隐藏预热。关闭预热后的首个 10 张可见任务约 `121.2 s`，没有消除冷启动，只把它转移给评委可见的第一次点击。
- 输入目录仅保留原始 5000 张和最终 Top300。`showcase_final_300_order.tsv` 保证质量最优 100 张优先进入前 100 个发送位次，IQ/QPSK 共用同一顺序。
